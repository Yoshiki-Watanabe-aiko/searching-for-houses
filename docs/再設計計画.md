# 物件検索通知システム 再設計計画

**作成日**: 2026-09-01
**位置づけ**: Go実装（v1）からPython実装（v2）への全面再設計の実施計画。Phase完了ごとに本書の進捗を更新し、`docs/requirements.md` へ確定仕様を反映していく。

---

## 1. 再設計の動機

ユーザー要望は「**優先度の高い条件順にランク付けし、その順にソートして家を探したい**」。現行システムにランク付けの概念はなく、条件モデルそのものの作り直しが必要になる。

### 1.1 現行システムの構造的問題

| 問題 | 内容 |
|---|---|
| ランク付け不能 | 全条件AND絞り込み型で、優先度・重みの概念がない |
| 条件対応の不均一 | 10サイト中6サイト（HOMES/GOO/APAMAN/MINIMINI/EHEYA/SMOCCA）が設備条件をURLに反映できない |
| 条件が黙って無視される | 上記6サイトでは条件が素通りし全件通知される。ローカル判定は現行コードに存在しない |
| 課題#1〜#3の温床 | いずれも「サイト側の検索フォームで絞る」方針に起因 |

### 1.2 調査で判明した既存ドキュメントの誤り（本計画で訂正する）

| 箇所 | 誤りの内容 | 実測による事実 |
|---|---|---|
| `docs/requirements.md` 2章 | ATHOMEを「HTTP + goquery」と記載 | `cmd/main.go:82` で go-rod 使用。**Playwright必須は5サイト**（ATHOME/EHEYA/NIFTY/APAMAN/SMOCCA） |
| `docs/課題管理表.md` #4 | 「SUUMO・LIFULL HOMESは売買4種別ともスクレイパー実装が完了」 | **売買コードは1行も存在しない**。SUUMOは `bs=040`（賃貸固定）のみ。完了しているのは検索フォームのPlaywright調査だけ |
| `docs/詳細設計書/資料_サイト別検索条件一覧.md` 1208行 | 「goo不動産 売買フォーム未取得」 | 本文646/675/712/767行に売買4種別の実URLと全パラメータが記載済み。冒頭注記も「goo完全取得」。**マトリクスの更新漏れ** |

### 1.3 現行システムの稼働状況

DB実測: `properties` 24,944件に対し `last_seen_at` の最大値が **2026-06-19**（約2.5ヶ月前）。タスクスケジューラにも登録なし。**現行システムは既に停止しており、並行運用期間は不要**。

---

## 2. 確定した方針

| 論点 | 決定 |
|---|---|
| 実装言語 | **Python**（Playwright / httpx＋lxml / SQLAlchemy 2.x＋psycopg / Alembic / pydantic / uv） |
| DB・通知 | PostgreSQL・Discord Webhook を継続 |
| ランク方式 | **MUST（未充足なら除外）＋ WANT（重み付き加点）の合計スコア降順** |
| 詳細ページ取得 | 初回のみ全件取得。以降は既存物件の更新確認と新着のみ |
| 通知 | 新着・価格変動は即時個別通知 ＋ 日次スコア上位Nのランキングダイジェスト |
| 対象種別 | **賃貸＋売買4種別（新築マンション含む）の5種別すべて** |
| 対象サイト | **11サイト**（既存10 ＋ 賃貸EX を `CHINTAI_EX` として追加）。Phase 3 で MINIMINI の取得手段が無いことが確定し、実装は10サイト |
| 着手順序 | **賃貸を Phase 5 まで完成させてから売買（Phase 6〜8）** |
| 既存DB | pg_dump アーカイブ後に作り直す（移行しない） |
| 旧資産退避先 | `F:\backups\searching-for-houses-legacy\` |
| 種別横断ランキング | **作らない**。1パターン=1種別を維持 |

---

## 3. アーキテクチャの転換

### 3.1 「サイト側で絞る」→「サイト側は粗く取り、判定・採点はローカル」

**この転換はランキング導入と論理的に不可分**である。WANT条件（オートロック無しでも減点で残ってほしい）をサイト側フィルタに渡すと、対応サイトでは物件が除外されて順位に現れず、非対応サイトでは素通りする。ランキングを入れる以上、設備条件のURL反映は全サイトで使ってはならない。

**サイトに渡すのは「エリア＋物件種別＋価格上限（バッファ付き）」の3つだけ。**

| 観点 | 旧方針 | 新方針 |
|---|---|---|
| 条件対応の均一性 | サイトごとにバラバラ（課題#1〜#3） | 全サイト同一の判定器 |
| 保守性 | 10サイト×条件マッピングの維持 | サイト別コードは「取得と解析」だけ |
| 取得件数 | 少ない | 増える（2段判定で緩和） |
| WANT条件との整合 | 不可能 | 自然に実現 |

副産物として課題#1（ABLE/SMOCCAの市区必須）は市区自動展開で解消、課題#2・#3は「対応不要」としてクローズできる。

### 3.2 2段判定によるコスト緩和

1. 賃料・間取り・面積・築年・徒歩は全サイトの**一覧ページに載っている**。ここでMUST判定し、`fail` の物件は詳細ページを取得しない
2. 既知物件（`external_id` 一致）は詳細を再取得しない
3. 市区町村指定で母集団自体を縮小できる

### 3.3 MUST判定の3値化

MUST判定は `pass` / `fail` / `unknown` の3値とし、**詳細取得をスキップするのは `fail` のみ**。

売買では管理費・修繕積立金・権利形態が一覧に出ず、詳細を見ないと判定できない。この問題は賃貸の敷金礼金でも潜在しているため、**Phase 1 から3値で実装する**。

---

## 4. スコアリングモデル

### 4.1 YAMLスキーマ v2

```yaml
name: "東京賃貸一人暮らし"
property_type: "CHINTAI"
webhook_ref: "CHINTAI_ALONE"       # .env の DISCORD_WEBHOOK_CHINTAI_ALONE を参照
sites: [SUUMO, HOMES, ATHOME, GOO, ABLE, MINIMINI, EHEYA, NIFTY, APAMAN, SMOCCA, CHINTAI_EX]

search:                             # サイト側へ渡す唯一の条件
  prefectures: ["東京都", "千葉県", "埼玉県", "神奈川県"]
  cities: []                        # 空ならABLE/SMOCCAは都道府県内全市区へ自動展開
  rent_max_hint: 90000              # MUST上限の2〜3割増し（管理費別サイト対策のバッファ）

must:                               # 未充足なら除外
  rent_total_max: 70000
  layouts: ["1LDK", "2K", "2DK", "2LDK", "3LDK"]
  area_min: 30.0
  walk_minutes_max: 20
  features: []
  unknown_policy: keep              # 判定不能なMUSTを keep=通す / drop=除外

want:
  features:                         # 該当なら weight 満点を加点
    - { code: INT_LAUNDRY,   weight: 10 }
    - { code: BATH_SEPARATE, weight: 9 }
    - { code: SEC_AUTOLOCK,  weight: 8 }
  numeric:                          # 線形正規化
    - { metric: rent_total, weight: 10, best: 50000, worst: 70000 }
    - { metric: area_sqm,   weight: 6,  best: 45,    worst: 30 }
    - { metric: age_years,  weight: 4,  best: 0,     worst: 30 }

ranking:
  top_n: 15
  digest_group: null                # 同一グループのパターンをダイジェストに並記（スコアは混ぜない）
```

### 4.2 スコア計算

- 正規化: `s = clamp((worst - x) / (worst - best), 0, 1)`（方向は metric ごとに定義）
- 合計: `score = 100 × Σ(wᵢ × sᵢ) / Σ(wᵢ)` の0〜100点
- 単位の異なる条件は `s` で無次元化されるため、weight だけで相対優先度を表現できる
- **欠損metricは分子・分母の双方から除外して再正規化**し、内訳に `"missing": true` を記録（新築マンションの価格未定を0点扱いにすると不当に沈むため）
- WANTの判定不能は0点＋「未確認」表示。中間値補完はしない
- **決定性**: 条件コード順にソートしてから加算（`PYTHONHASHSEED` を変えたサブプロセス間比較でテスト）

### 4.3 説明可能性

`t_property_scores.score_breakdown`（JSONB）に全項目の `{code, name, weight, s, points, status: hit|miss|unknown}` を保存。

- 即時通知: スコア・パターン内順位・得点上位3項目・未確認項目数
- ダイジェスト: 順位・スコア・価格・間取り/面積・市区・URL（Discord embed制約 description 4096字/1embed、6000字/1メッセージ、10embed/1メッセージ）

### 4.4 再スコアリング

スコアは「DB保存済みの物件属性＋抽出済みfeatures」からの純関数のため、**再計算はネットワーク不要のDBバッチ**。YAMLのスコア関連部分のハッシュを `config_hash` として保存し、不一致なら自動再スコア。CLI `rescore` も用意する。

### 4.5 種別ごとのmetric体系

物件種別は実質**3ファミリ**（賃貸／マンション売買／戸建て売買）に分かれる。

| metric | 方向 | 賃貸 | 新築M | 中古M | 新築K | 中古K |
|---|---|:---:|:---:|:---:|:---:|:---:|
| `rent_total` | 低いほど良 | ○ | - | - | - | - |
| `price` | 低いほど良 | - | ○ | ○ | ○ | ○ |
| `monthly_cost`（管理費+修繕積立金） | 低いほど良 | - | △ | ○ | - | - |
| `area_sqm`（専有面積） | 高いほど良 | ○ | ○ | ○ | - | - |
| `building_area_sqm` | 高いほど良 | - | - | - | ○ | ○ |
| `land_area_sqm` | 高いほど良 | - | - | - | ○ | ○ |
| `age_years` | 低いほど良 | ○ | - | ○ | - | ○ |
| `walk_minutes` | 低いほど良 | ○ | ○ | ○ | ○ | ○ |

- **戸建てに `area_sqm` を流用しない**。専有面積が存在せず土地面積・建物面積の2軸になるため、混線と名寄せ事故を避けて別metricにする
- **坪単価・㎡単価は metric にしない**。price と area に既に weight を配れる以上、二重に重みが掛かり解釈が濁る。`price_per_sqm` は表示用の派生カラムとしてのみ保持

### 4.6 スキーマ表現（pydantic）

**discriminated union を3ファミリ粒度で採用**する。`property_type` を discriminator に `ChintaiPattern` / `MansionBuyPattern` / `KodateBuyPattern` へ分岐し、共通部（name / search / webhook_ref / want.features）は基底モデルに置く。

metric・MUST項目は **MetricRegistry**（metric名 → 適用種別集合・抽出元カラム・単位・方向）で一元管理し、`model_validator` がレジストリを引いて検証する。種別追加はレジストリ1行で済む。

5種別を5クラスに割らない理由: 新築/中古の差は `age_years`・価格未定・リノベ関連の数項目だけで、クラスを分けるほどの構造差がない。

---

## 5. 設備情報のローカル抽出

### 5.1 パイプライン

1. **原文保存**: 詳細ページの設備ブロックを**テキストのまま** `t_properties.raw_features_text` に保存（詳細HTML全体は保存しない）
2. **辞書マッチング**: NFKC正規化→小文字化→トークン化→辞書照合→`t_property_features` 生成

**原文保存が要**。辞書を改善したら再スクレイピングせずDB内の原文から全件再抽出できる（`re-extract`）。これがないと辞書運用が回らない。

### 5.2 辞書の持ち方

**Git管理YAML（正）→ DB同期** を採用。`data/feature_dictionary.yaml` を `sync-dict` コマンドで `m_condition_synonyms` へupsertする。

```yaml
SEC_AUTOLOCK:
  patterns: ["オートロック", "オートロック付"]
  negative_patterns: ["オートロックなし"]
  site_overrides:
    SUUMO: ["オートロック"]
```

Git履歴でdiffレビューでき、実行時はDB参照でJOINできる。**賃貸ブロックと売買ブロックの2部構成**にする（証明書・性能評価系の語彙は賃貸と別体系のため）。

### 5.3 未知表記の運用ループ

どのパターンにもマッチしなかったトークンを `t_unknown_tokens`（token・site・出現回数・例示URL）へ記録。`report-unknown` で出現回数順に一覧し、辞書YAMLへ追記→`sync-dict`→`re-extract` で反映する。

---

## 6. クロスサイト名寄せ

ランキング上位が同一物件のサイト違いで埋まるため、**将来課題ではなく本体要件**とする。

- **キー**: 正規化住所＋間取り＋面積＋階数 の SHA256（`dedup_key`）
- **完全一致のみ自動グループ化**。曖昧一致は候補フラグ止まり（名寄せの誤爆はランキングから物件を1件消すことを意味し、偽陽性のコストが高い）
- **建物名はキーに含めない**（賃貸では非公開・伏字が多く偽陰性の主因）
- **種別ファミリで構成要素を変える**:
  - 賃貸/マンション: 住所＋間取り＋専有面積＋階数
  - 戸建て: 住所＋土地面積＋建物面積＋間取り（階数・部屋番号の概念がない）
  - ハッシュ入力に**ファミリ識別子を混ぜる**（分譲賃貸が「賃貸」と「中古売買」で二重グループ化されるのを防ぐ）
- **代表選定**: 月額/価格が最安 → 設備抽出数が最多 → サイト優先順
- **スコアはグループ内の抽出情報の和集合**で計算（サイトAで判明した設備＋サイトBで判明した設備をマージ）
- **通知**: グループ単位で重複抑制。代表より安い掲載が出たら「他サイトで安値掲載」として通知

---

## 7. DB設計

### 7.1 新旧テーブル対応

> ⚠ **本節の `t_properties` 系は Phase 5 でリネームした**（→ 課題#30）。
> `t_properties` → `t_listings` / `t_property_features` → `t_listing_features` /
> `t_property_scores` → `t_listing_scores` / `t_property_groups` → `t_listing_groups`。
> 「物件」が掲載と住戸のどちらを指すか曖昧だったため（→ CONTEXT.md）。
> 以下は Phase 0 時点の記録として残す。

| 旧 | 新 | 備考 |
|---|---|---|
| `property_type_master` | `m_property_types` | |
| `site_master` | `m_sites` | 賃貸EX（`CHINTAI_EX`）を追加し12サイト |
| `condition_category_master` | `m_condition_categories` | 売買用に CERT・LAND の2カテゴリ追加 |
| `condition_master` | `m_conditions` | 128 → 145前後（売買固有条件を追加） |
| `condition_property_type_map` | `m_condition_property_types` | |
| `site_condition_map` | **廃止** | サイト側設備フィルタ全廃のため不要 |
| `cities` | `m_cities` | |
| `city_site_mappings` | `m_city_site_values` | **縦持ちへ転換**（city_id × site_id × value）。ADR 0001 を撤回 |
| — | `m_condition_synonyms` | 新設（抽出辞書） |
| `properties` | `t_properties` | 新カラム多数（下記7.2） |
| — | `t_property_features` | 新設（抽出結果） |
| — | `t_property_scores` | 新設（score・breakdown JSONB・config_hash） |
| — | `t_property_groups` | 新設（名寄せグループ） |
| `notifications` | `t_notifications` | `score_at_notify` 追加 |
| — | `t_ranking_digests` | 新設（ダイジェスト送信履歴） |
| `scrape_logs` | `t_scrape_logs` | |
| — | `t_scrape_runs` | 新設（実行チェックポイント） |
| — | `t_unknown_tokens` | 新設（未知表記） |

DB_CONVENTIONS 準拠: `m_`/`t_` 接頭辞、日本語コメント、監査カラム（`created_at`/`updated_at`）を最終列。列順・コメントは回帰テストで担保する。

**`m_city_site_values` を縦持ちに転換する理由**: 賃貸EX追加で「サイトを増やすたびにDDL変更が要る」問題が実際に顕在化した。DB規約上、列追加は監査カラムを最終列に保つためテーブル再作成を伴う。縦持ちなら以後のサイト追加は行の挿入だけで済む。ADR 0001（ワイドテーブル採用）は「サイト数が固定でないと判明したため撤回」として記録し直す。

### 7.2 `t_properties` の売買対応

**ハイブリッド方式**（metric/MUSTの入力になる数値は型付き列、正規化未確立の文字列系はJSONB）を採用。全部JSONBにするとスコアリングエンジンが型保証のない値を読むことになりMUST判定の信頼性が下がる。全部列にすると接道「南6m公道」のような表記揺れの激しい項目でスキーマ変更が頻発する。

追加列（**Phase 0 のDDL初版に含める**。後からの列追加はテーブル再作成になるため）:

| 列 | 用途 |
|---|---|
| `land_area_sqm` NUMERIC | 土地面積㎡（戸建て。metric入力） |
| `building_area_sqm` NUMERIC | 建物面積㎡（戸建て。metric入力） |
| `mgmt_fee_monthly` BIGINT | 管理費 円/月（マンション売買） |
| `repair_reserve_monthly` BIGINT | 修繕積立金 円/月（マンション売買） |
| `price_min` / `price_max` BIGINT | 価格レンジ（新築対応） |
| `price_per_sqm` NUMERIC | ㎡単価（マンションのみ。表示用派生値） |
| `raw_features_text` TEXT | 設備ブロック原文 |
| `dedup_key` / `group_id` | 名寄せ |
| `detail_fetched_at` | 詳細取得済み判定 |
| `type_specific_attrs` JSONB | 接道・建ぺい率/容積率・権利形態・引渡時期・建築条件付きフラグ等 |

### 7.3 新築物件の掲載粒度

賃貸・中古が「1物件=1住戸」なのに対し、**新築マンション・新築分譲戸建ては「1物件=1棟/1プロジェクト」で価格がレンジ表示**（「3,500万円台〜」「価格未定」）される。

- 新築系は棟/プロジェクト単位で1レコード。`price` にレンジ下限、`price_min`/`price_max` にレンジ
- 価格未定は `price NULL` ＋ `type_specific_attrs.price_undecided = true`
- スコアはレンジ下限で計算し内訳に `"range": true` を記録。価格未定は price metric 欠損として再正規化
- 通知は棟単位（「新価格帯検出」）。住戸タイプ別の追跡はしない

### 7.4 旧データの扱い

**移行せず、シードモードで副作用を消す。**

通知履歴を捨てると再掲載が全部「新着」として再通知される問題は、**初回全件取得を通知なしの記録専用モード（`--seed`）で走らせる**ことで構造的に発生しなくなる。パターン新規追加時・長期停止からの再開時にも使える汎用機能とする。

旧DBは pg_dump でアーカイブ済み（`F:\backups\searching-for-houses-legacy\db_20260901.dump`、11テーブル・2.5MB）。

### 7.5 テストDB分離（課題#7）

`searching_for_houses_test` を作成し `.env` に `DATABASE_TEST_URL` を追加。統合テストは未設定時スキップ。DB・ロール作成は `~/.claude/.env` の管理者パスワードを読む冪等スクリプトで行う。

---

## 8. 通知設計

| 系統 | トリガー | 形式 |
|---|---|---|
| 新着 | DB未登録の物件を検出（グループ単位で重複抑制） | 個別Embed。スコア・順位・得点上位3項目 |
| 価格変動 | 前回価格と差分 | 個別Embed |
| ランキングダイジェスト | 日次 | 1メッセージにスコア上位N件 |
| エラー | サイト失敗・YAML読込失敗 | `.env` の `DISCORD_WEBHOOK_ERRORS` |

**種別横断ランキングは作らない。** 価格帯も面積の意味も違う種別のスコアは正規化基準が異なり、混ぜると数字が意味を失う。「中古マンションと中古戸建てを並べて見たい」ニーズには `digest_group` によるダイジェストの**セクション並記**で応える（スコアは混ぜずパターンごとに上位Nを並べる）。実装差分は通知フォーマッタのみ。

---

## 9. 実行・運用

### 9.1 初回全件取得

規模: MUST事前判定通過が2〜3万件、サイト内直列2.5秒間隔・サイト間並列で**3〜8時間**の見込み。

- **エージェントのバックグラウンドから起動しない**（パイプ詰まりで例外なく停止する）。`scripts/run_initial_scan.ps1` を用意し `Start-Process` で切り離し、**stdout/stderrを別ファイル**へ落とす
- 進捗は `t_scrape_runs` とログファイルで観測。**停止判断をCPU使用率でしない**
- 日本語コメント入り `.ps1` は BOM付きUTF-8 で保存

### 9.2 中断・再開

`t_scrape_runs` に `(run_id, mode, pattern, site, phase, cursor, status)` を記録。詳細取得は `detail_fetched_at IS NULL` のキュー型なので**再開はSQLの自然な帰結**になる。upsertは全て冪等。

### 9.3 レート制御

サイトごとに `min_interval_sec`（既定2.5秒＋±30%ジッタ）・`max_pages_per_run`・`daily_request_cap`。429/5xxは指数バックオフ、連続失敗でサイト打ち切り＋エラー通知。robots.txt は起動時取得しDisallowならスキップ。

### 9.4 タスクスケジューラ

- タスク用スクリプトは `-Wait` で子として待たせる（`Start-Process` 切り離し版の流用禁止。タスクが即完了扱いになり二重起動する）
- 登録は `Register-ScheduledTask` でなく **XMLをUTF-16で書き出して `schtasks /create /XML`**
- 構成: ①毎時 増分スキャン ②毎日9:00 成約確認 ③毎日20:00 ダイジェスト ④毎日3:30 pg_dump（課題#8解消）
- すべて `.venv\Scripts\python.exe` をフルパス起動

---

## 10. セキュリティ

- **Webhook URLを `.env` へ集約**（`DISCORD_WEBHOOK_{用途}`）。YAMLは `webhook_ref` で参照し、未定義参照は起動時バリデーションでエラー
- 副産物として **`configs/*.yaml` をGit管理に戻せる**（検索条件の変更履歴が残る）
- 移行時にDiscord側でWebhookを**再発行（ローテーション）**することを推奨
- `.env` の空値項目にインラインコメントを書かない（python-dotenvの罠）

---

## 11. Phase構成

**賃貸を Phase 5 まで貫通させて「早く動くもの」を先に確保**し、売買は Phase 6〜8 に置く。実運用中の条件は賃貸のみ（`chintai_alone.yaml`）のため、売買を並行させて賃貸の完成を遅らせる理由がない。**ただしDDLと型スキーマの骨格は Phase 0 で売買込みにする**。

| Phase | 内容 | 完了条件 |
|---|---|---|
| **0** 土台 ✅**完了 2026-09-01** | 資産退避、Pythonプロジェクト初期化、新DDL（12サイト・5種別・売買カラム・縦持ちマッピング込み）、MetricRegistry と discriminated union の骨格（売買ファミリはプレースホルダ）、既存ドキュメントの誤り訂正 | DDLが適用でき pytest が通る |
| **1** 縦切り ✅**完了 2026-09-01** | SUUMO賃貸で取得→MUST3値判定→詳細→抽出→スコア→通知まで貫通。辞書初版（賃貸80条件/257パターン） | **ランキングがDiscordに届いた**（上位15件・送信成功） |
| **2** HTTP展開 ✅**完了 2026-09-01** | HOMES/GOO/ABLE ＋ 賃貸EX（base_url・エリア指定方式確定）。市区自動展開・`coverage` コマンド。**MINIMINI は reCAPTCHA のため Phase 3 送り** | **フィクスチャテスト緑（274件）＋抽出充足率を5サイトで実測** |
| **3** Playwright展開 ✅**完了 2026-09-02** | ATHOME/EHEYA/NIFTY/APAMAN/SMOCCA を実装。**HTTP降格の再検証で5サイトとも降格でき、Playwright依存を撤去**。MINIMINI は素のブラウザでも reCAPTCHA を通れず未実装 | **賃貸10サイトで縦切り貫通**（364テスト緑・9サイトで充足率を実測） |
| **4** 名寄せ ✅**完了 2026-09-02** | dedup_key・グループ化・代表選定・featureマージ。賃貸EXのユニーク物件率実測 | **ランキングがグループ単位で出た**（301掲載→253グループ・427テスト緑） |
| **5** 賃貸本運用 ⏳**再設計中 2026-09-02** | 初回全件スキャン（シードモード）、タスクスケジューラ4本、pg_dumpバックアップ | **賃貸が旧システムを完全代替**（スクリプトは完成・検証済み。初回スキャンの実行とタスク登録が残る → §16.7） |
| **5A** マスタ全国化 ✅**完了 2026-09-03** | 市区町村マスタの正典を総務省コード表へ移し47都道府県を網羅、誤コードの訂正と部分ユニーク索引、政令市の二重展開の排除、`resolve-cities` バックフィル | **全国どの市区でも検索URLを組み立てられる**（1,918行・`jis_code` 100%・帯の採点は不変） |
| **5B** サイト側MUST ⏳**基盤とSUUMOのみ完了 2026-09-03** | サイトごとの検索可能条件を MUST に限りURLへ載せる。離散選択肢の丸め・0件事故の検出・`site_filters` の設定スキーマ | フィルタ有無で **MUST通過集合が同一**（取りこぼし0の実測）。無効値0件の検出が実際に発火する（SUUMO で達成。HOMES/GOO/APAMAN が残る） |
| **5C** 通勤時間 ✅**完了 2026-09-03** | 駅マスタ、掲載の駅同定、所要時間の自前算出とキャッシュ、`commute_minutes_max`（MUST）と `commute_minutes`（WANT）の配線 | **上位15件すべてに通勤時間が付き、内見したい物件が並んだ**（→ §16.11）。⚠ Google Maps は日本の公共交通経路を返さないため自前計算へ切り替え、完了条件の「±5分以内」は「実感と合うか」へ変更した（→ ADR 0016） |
| **5D** 実ダイヤ ✅**完了 2026-09-03** | 通勤時間を回帰式から **NAVITIME の実ダイヤ**へ置き換える。経路の原文と乗車区間（駅間）の実所要時間をDBへ貯める。**ODPT は使わない**（登録待ちと未参加事業者の穴 → ADR 0017） | **上位15件の通勤時間が実ダイヤの値になった**（全1,155駅を4時間48分で取得・navitime 1,126駅。残る29駅は照合が弾いたもので上位15件に3件混ざる → 課題#34） |
| **5E** 取得の回復 ✅**完了 2026-09-03** | 取得数に上限があるサイト（HOMES 5・ATHOME 4 リクエスト/回）を**市区ローテーション**で回す。HOMES の間隔を 2.5秒へ戻し、ATHOME を有効化。前提として両サイトの市区スラグを収集する（→ 課題#36・#21） | **上限を踏んでも次の市区・次の実行へ進むことを実地で確認した**（HOMES 掲載10→28件・帯の交代とカーソル前進を実測・646テスト緑）。⚠ ATHOME は23区帯で MUST 1段目通過0件（面積下限を渡せていないため → §16.14） |
| **5F** UR賃貸 ✅**実装完了 2026-09-03** | UR賃貸住宅を追加する。`SiteFetcher` の POST 対応、robots.txt 403 の明示的な扱い（→ ADR 0019）、団地→住戸→住戸詳細の**3段**取得（→ 課題#37） | **配線は貫通**（一覧→MUST→詳細→設備36件→スコアまで本番DBで確認・701テスト緑）。⚠ **実データでは MUST 通過0件**（UR は対象市区で7〜11万円台が中心・`layouts` に3DKが無い）。緩めるかはユーザー判断 → §16.17 |
| **5G** 自社物件系 ⏳**計画のみ 2026-09-03** | レオパレス21 → D-room。既存10サイトと在庫が重ならない見込みが高い順に先行させる | フィクスチャ緑＋`coverage`／`dedup-stats` の実測。**帯内ユニーク率で採用/棄却を判断し記録する** |
| **5H** ポータル系 ✅**完了 2026-09-04** | ハウスコム → ホームメイト → CHINTAI.net。D-room も含む4サイト。CHINTAI.net は robots の解析欠陥（課題#43）を先に直してから実装した | **4サイトとも実装し `dedup-stats` で採否を判断した**（ハウスコムのユニーク率81%は全サイト中で最高 → §16.20・§16.21） |
| **6** 売買縦切り | 売買metric・YAMLファミリ2種、SUUMO/HOMESの売買一覧・詳細DOM解析（**新規実装**）、新築の価格未定・レンジ・棟粒度対応、売買辞書の育成開始 | 中古Mが通知まで貫通。新築Mがクラッシュせず処理される |
| **7** 売買展開 | 調査3件（ATHOME新築Mのパラメータ体系／NIFTY売買一覧DOM／goo売買疎通検証）＋ATHOME→NIFTY→GOO実装。**時間箱で打ち切り基準を事前定義** | 採用セルで取得とMUST判定が動く |
| **8** 売買本運用 | 売買dedup_keyの実測検証、digest_group運用開始、課題#4・#5クローズ | 重複通知率が許容内 |

### 11.1 サイト×種別の対応マトリクス

凡例: **A**=パラメータ判明済 / **A△**=URL判明・一部未取得 / **B**=未調査 / **C**=非対応

| サイト | 賃貸 | 新築M | 中古M | 新築K | 中古K |
|---|:---:|:---:|:---:|:---:|:---:|
| SUUMO | A | A | A | A | A |
| HOMES | A | A | A | A | A |
| ATHOME | A | **B** | A | A | A |
| GOO | A | A | A | A | A |
| NIFTY | A | A△ | A△ | A△ | A△ |
| EHEYA / ABLE / MINIMINI / APAMAN / SMOCCA | A | C | C | C | C |
| CHINTAI_EX | A | C | C | C | C |

実装対象は 55セル中 **31セル**（賃貸11 ＋ 売買4種別×5サイト）。

**新アーキテクチャの恩恵**: フォームに渡すのはエリア・種別・価格上限だけなので、**こだわり条件パラメータの未取得（NIFTY売買のA△等）は実装ブロッカーにならない**。必要なのは一覧URL構築と一覧/詳細DOM解析だけ。調査タスクは3件に集約される。

### 11.2 テスト方針（課題#7）

**HTMLフィクスチャ方式**。各サイトの一覧・詳細ページの実HTMLを `tests/fixtures/{site}/` に保存し、パーサ・抽出・スコアリングをネットワークなしでユニットテストする。

加えて「実装済みだが未配線」を防ぐため、**実データ充足率の実測コマンド**（サイト別の設備抽出数分布・数値カラム非NULL率）をPhase完了条件に組み込む。

---

## 12. 工数とリスク

賃貸のみの計画を 1.0 とすると全体で **約1.7〜2.0倍**。ただし**賃貸の完成時期（Phase 5）は変わらない**構成にしてある。

| リスク | 緩和策 |
|---|---|
| ATHOME新築Mのタグリンク形式が通常のクエリ検索に変換できない | 時間箱調査。不成立なら「価格上限なしで一覧取得→全てローカル判定」に切替（新アーキテクチャなら成立する） |
| goo売買が資料本文どおりに動かない | gooを売買最後尾に配置済み。掲載重複が多く落としても情報損失は小さい |
| 売買設備テキストが賃貸と別体系 | 辞書を2部構成に分離。シードモードで未知トークンを先に貯めてから通知有効化。賃貸辞書には手を入れない |
| 新築の価格未定・レンジ・棟粒度がパイプラインを壊す | 粒度方針をPhase 0でスキーマに織込み、Phase 6の完了条件に「価格未定物件の無事故処理」を明記 |
| 戸建て・新築の名寄せ成功率低下 | 種別別dedup_key＋Phase 8で実測。閾値未達ならサイト内重複排除のみに縮退 |
| 賃貸EXのユニーク物件寄与が小さい | 観測モード先行でユニーク率を実測してから通知有効化 |
| 売買は掲載回転が遅く新着通知が少ない | 売買は価格変動通知とダイジェストが主役になる旨を運用設計に明記 |

---

## 13. 課題管理表への反映

| 課題 | 新設計での扱い |
|---|---|
| #1 ABLE・SMOCCA市区必須 | **解消**（市区自動展開） |
| #2 スクレイパー未実装条件 | **クローズ**（サイト側設備フィルタ全廃で対応不要化） |
| #3 ATHOME設備AND検索 | **クローズ**（同上） |
| #4 売買4種別の未対応 | Phase 6〜8で対応。**記述の誤りを先に訂正**（SUUMO/HOMESも売買未実装） |
| #5 賃貸EX | **対応する**（`CHINTAI_EX` として11サイト目） |
| #6 m_/t_接頭辞非準拠 | **解消**（新スキーマで準拠） |
| #7 テストDB・自動テスト未整備 | **解消**（`DATABASE_TEST_URL`＋フィクスチャテスト） |
| #8 pg_dump定期バックアップ未設定 | **解消**（Phase 5でタスク登録） |
| 新規 | 名寄せ精度の実測、辞書カバレッジ向上、goo資料の矛盾訂正 |

---

## 14. ドキュメント更新計画

| ドキュメント | 対応 |
|---|---|
| `docs/requirements.md` | 全面改訂（v2）。Phase 0で骨子、各Phase完了時に確定記述へ |
| `docs/詳細設計書/` | 再編: `01_サイト取得設計.md` / `02_設備抽出辞書設計.md` / `03_スコアリング設計.md` / `04_名寄せ設計.md`。現行 `01_検索条件マッピング設計.md` は廃止。`資料_サイト別検索条件一覧.md` は保持（矛盾箇所を訂正） |
| `docs/adr/` | 0002 Go→Python移行 / 0003 ローカル判定への転換 / 0004 スコアリングモデル / 0005 名寄せキー / 0006 旧DB非移行とシードモード / 0007 抽出辞書 / 0008 robots.txt・レート制御 / 0009 ADR0001撤回（縦持ち転換） |
| `CLAUDE.md` | 技術スタック更新 |

---

## 15. ユーザー確認事項（2026-09-01 確定）

| # | 論点 | 決定 |
|---|---|---|
| 1 | WANT features の weight 初期値 | **案A バランス型**。v1 の25条件のうち `STRUCT_RC`/`STRUCT_SRC` を `any_of` で統合し、`FEAT_WITH_PHOTO`/`FEAT_WITH_LAYOUT` を除外した22項目・weight合計110 |
| 2 | 数値条件の weight と best/worst | **数値45%**（合計90）。`rent_total` 40（5万〜7万）/ `area_sqm` 20（45〜30㎡）/ `age_years` 15（0〜30年）/ `walk_minutes` 15（3〜20分）。**worst は MUST の上限/下限に一致**させた |
| 3 | ダイジェストの送信時刻と件数 | **毎日20:00・上位15件** |
| 4 | Webhookローテーション | **実施する**（ユーザー側の手作業。Discord で再発行 → `.env` を更新 → `configs/_v1/chintai_alone.yaml` を削除） |
| 5 | 売買サイトの取捨選択権限 | Phase 7 着手時に再確認する（未着手） |

### 15.1 Phase 1 で採用した設計上の追加判断

| 判断 | 理由 |
|---|---|
| `want.features` に **`any_of`（排他グループ）** を追加 | RC と SRC に別々の weight を振ると片方は必ず miss になるのに分母には両方が乗り、全物件のスコア上限が構造的に下がる。順位は壊れないが 0〜100点という数字の意味が濁る |
| `FEAT_WITH_PHOTO` / `FEAT_WITH_LAYOUT` を WANT から除外 | 掲載の充実度であって物件の質ではない。加点すると写真の無い優良物件が平凡だが写真付きの物件の下に沈む |
| 閾値条件を型付き列から導出（`source='DERIVED'`） | 「2階以上」は文字列照合で表現できない。辞書に頼ると weight 5 が常に miss になり無駄になる |
| 辞書照合を**本文全体への部分一致**にした | 区切り文字がサイトごとに違い、中黒で切ると「バス・トイレ別」を取りこぼす |
| 雛形YAMLを `configs/examples/` へ退避 | `configs/` 直下にあると実パターンとして走り、同じWebhookへ二重通知される（実際に発生した） |

## 16. 完了済み作業

### 16.1 準備（2026-09-01）

- 旧DBのpg_dumpアーカイブ（`F:\backups\searching-for-houses-legacy\db_20260901.dump`、11テーブル・2.5MB。`pg_restore -l` で検証済み）
- 旧実行ファイル退避（`house-notifier.exe` 21MB）
- `legacy-go` ブランチ ＋ `v1-go-final` タグ作成（mainと同一コミット。Goコードはremoteにも保全済み）
- 実行環境確認（Python 3.14.4 / uv 0.9.7 / PostgreSQL 18）

### 16.2 Phase 0 土台（2026-09-01 完了）

**完了条件「DDLが適用でき pytest が通る」を達成**（17テーブル適用済み・49テスト緑）。

| 区分 | 内容 |
|---|---|
| Pythonプロジェクト | `pyproject.toml`（uv / hatchling）・`src/house_search/` パッケージ・CLI `house-search`（9サブコマンド。Phase 0 実働は `db-seed` と `validate-config`、残りは実装予定Phaseを告げて終了コード2で終わる） |
| DBスキーマ | Alembic 初期マイグレーション1本で **m_* 8テーブル ＋ t_* 9テーブル**。売買カラム・`m_city_site_values` 縦持ち・生成列 `rent_total` を初版に含む |
| マスタデータ | `db/seed/*.sql`（冪等）。5種別 / 12サイト / 19カテゴリ / **148条件**（v1の128＋売買固有20） / 487マッピング / 947市区町村 / 1181サイト検索値 |
| 型スキーマ骨格 | `config/metrics.py`（MetricRegistry: 8 metric ＋ 12 MUST項目。種別ごとの適用可否と「一覧だけで判定できるか」を保持）・`config/pattern.py`（`property_type` を discriminator にした3ファミリの discriminated union、`config_hash`） |
| テストDB | `searching_for_houses_test` 作成・`DATABASE_TEST_URL` 追加。`scripts/setup_db.ps1`（冪等・`~/.claude/.env` から資格情報を読む・BOM付きUTF-8） |
| 旧資産の撤去 | 旧11テーブルを drop（マスタ移送の突き合わせで欠落0を確認後）。Go実装・v1 SQL・v1タスクスケジューラスクリプトを削除 |
| セキュリティ | Webhook URLを `.env` へ集約（`DISCORD_WEBHOOK_{論理名}`）。YAMLは `webhook_ref` で参照し、未定義参照は `validate-config` でエラーにする |
| ドキュメント | 要件定義書をv2骨子へ全面改訂／課題管理表を更新（#2・#3 クローズ、#6・#7 解決、#9〜#12 新規起票）／調査資料の誤りを訂正／ADR 0002・0003・0006・0009 を追加し ADR 0001 を撤回 |

**Phase 0 で判明した追加の誤り**: 要件定義書のサイト別URL値フォーマット表で、
EHEYA・SMOCCA を「URLスラグ」、MINIMINI を「JIS5桁コード」としていたが実データは逆だった
（EHEYA・SMOCCA がJIS5桁コード、MINIMINI がスラグ）。要件定義書 §11.2 で訂正済み。

**Phase 1 への引き継ぎ**: v1形式の `chintai_alone.yaml` は `configs/_v1/` へ退避してある。
v2形式への変換には WANT の重み初期値と best/worst の確定（§15-1・§15-2）が必要。

### 16.3 Phase 1 縦切り（2026-09-01 完了）

**完了条件「ランキングがDiscordに届く」を達成**（上位15件のダイジェスト送信成功・183テスト緑）。

| 区分 | 内容 |
|---|---|
| 検索パターン | `configs/chintai_alone.yaml` を v2 形式で作成（課題#9 解消）。Webhook は `.env` 参照なのでGit管理下に置ける |
| スキーマ拡張 | `want.features` に `any_of`（排他グループ）を追加 |
| 取得 | `scrape/fetch.py`（レート制御・±30%ジッタ・429/5xx指数バックオフ・連続失敗打ち切り・日次上限・robots.txt）／`scrape/suumo.py`（一覧・詳細。**設備条件のクエリパラメータは送らない**） |
| 抽出 | `extract/`（NFKC正規化・本文全体への部分一致・否定パターン・サイト固有パターン・型付き列からの導出・未知表記収集）。辞書 `data/feature_dictionary.yaml` 賃貸80条件/257パターン |
| 採点 | `scoring/`（MUST 3値・2段判定・`any_of`・欠損metricの再正規化・決定性）。重みは案A（設備110/数値90） |
| 通知 | `notify/`（新着・価格変動のEmbed／ダイジェストのテキスト表／429リトライ／エラーチャンネル） |
| CLI | `scan`（`--seed`/`--full`/`--site`）・`check-sold`・`digest`（`--dry-run`）・`rescore`・`sync-dict`・`re-extract`・`report-unknown` を実装。残る未実装は `coverage`（Phase 2）のみ |
| テスト | 49件 → **183件**。実HTMLフィクスチャ（`tests/fixtures/suumo/`）でパーサを回帰テスト。`PYTHONHASHSEED` を変えたサブプロセス間でスコアの決定性を検証 |
| ドキュメント | 詳細設計書を再編（`01_サイト取得設計.md` / `02_設備抽出辞書設計.md` / `03_スコアリング設計.md`）し、廃止済みの `01_検索条件マッピング設計.md` を削除（課題#10 解消） |

**実測値（SUUMO・東京/千葉/埼玉/神奈川の各1ページ）**

| 指標 | 実測 |
|---|---|
| 一覧取得 | 177掲載 |
| MUST 1段目通過 | 22掲載（**詳細取得を87%削減**） |
| 一覧項目の充足率 | 賃料・管理費・敷金・礼金・面積・間取り・所在階・築年・徒歩・住所すべて100% |
| 設備抽出数 | 平均21件/物件（最小2・最大35） |
| 最終スコア | 上位59.7点〜、MUST通過34/36件 |

**Phase 2 への引き継ぎ**: 下記「Phase 1 で判明した課題」を参照。

### 16.4 Phase 2 HTTP展開（2026-09-01 完了）

**完了条件「フィクスチャテスト緑＋抽出充足率の実測」を達成**（274テスト緑・5サイトを実測）。

| 区分 | 内容 |
|---|---|
| エリア解決 | `scrape/area.py` を新設。アダプタが `requires_city` / `city_value_source` を宣言し、市区必須サイトは `search.cities` が空でも都道府県内の全市区へ展開する（課題#1 解消） |
| アダプタ | HOMES / GOO / ABLE / CHINTAI_EX を追加（計5サイト）。実HTMLフィクスチャで回帰テスト |
| サイト別UA | 既定UAを 403 で拒否するサイト向けに、アダプタが `user_agent` を宣言できるようにした |
| マスタ訂正 | `m_cities.jis_code` を473件補完・26件訂正。`m_sites` に賃貸EXの base_url と各サイトの注記 |
| CLI | `coverage` を実装（残る未実装コマンドは無くなった）。`scan` は `is_active=false` のサイトを通常実行では取りに行かない |
| 堅牢化 | 1ページ／1物件の解析失敗でサイト全体が止まらないようにした（HOME'S の空応答で実際に発生） |
| テスト | 183件 → **274件**。`tests/test_area.py` / `test_persist.py` / `test_scrape_{homes,goo,able,chintai_ex}.py` |

**実測値（足立区・葛飾区・八王子市の各1ページ／MUSTを緩めて詳細取得数を確保）**

| サイト | 物件 | 詳細済 | 設備平均 | 最小 | 最大 | 数値カラム非NULL率 |
|---|---:|---:|---:|---:|---:|---|
| SUUMO | 60 | 60 | 22.6 | 2 | 35 | 100%（階のみ98/97%） |
| GOO | 17 | 17 | 18.6 | 11 | 27 | 100% |
| HOMES | 6 | 4 | 14.8 | 11 | 17 | 100%（築年月・設備原文は67%） |
| CHINTAI_EX | 11 | 11 | 12.9 | 8 | 21 | 100% |
| ABLE | 6 | 6 | 9.3 | 6 | 11 | 100% |

#### Phase 2 で判明した想定外のこと

| 事象 | 対応 |
|---|---|
| **MINIMINI の一覧が reCAPTCHA のボット判定下** | 回避しない。Phase 3 で扱いを再判断（課題#18） |
| **LIFULL HOME'S が AWS WAF のボットチャレンジを返すようになった** | 突破しない。アダプタは残し観測（課題#17） |
| **`m_cities.jis_code` が947件中789件 NULL** | JIS系サイトは検索値をここから導くため致命的。実測で473件補完（課題#16に残り） |
| **大阪市・福岡市のJISコードが26件ずれていた** | 実測値で訂正 |
| **ABLE の賃料上限クエリが効かない** | 賃料昇順（`o=1`）で代替 |
| **賃貸EX の robots.txt がクエリ付きURLを全面禁止** | 価格上限を渡さずローカル判定。ページ送りはパス形式で対応 |
| **賃貸EX が同じURLで3種類の一覧レイアウトを返す** | クラスで拾って中身で分岐 |
| **賃貸EX の住所に都道府県が付かない** | `resolve_city` が一意な市区名なら前置なしでも引けるようにした |
| **HOMES / GOO の詳細に「非該当」条件が並ぶ** | 該当だけを原文に載せる（辞書が非該当を拾うのを防ぐ） |

**Phase 3 への引き継ぎ**: 未実装は Playwright 5サイト（ATHOME/EHEYA/NIFTY/APAMAN/SMOCCA）と
MINIMINI。EHEYA・SMOCCA の市区値の形式は `m_city_site_values` の実データが矛盾しているため、
アダプタ実装時に実測して確定すること（→ 詳細設計書 §3）。

### 16.5 Phase 3 取得サイトの展開（2026-09-02 完了）

**完了条件「賃貸11サイトで縦切り貫通」を、取得可能な10サイトで達成**
（364テスト緑・9サイトで充足率を実測）。MINIMINI だけは取得手段が無いことを実測で確定した。

#### 前提が覆った点（着手前の実測で判明）

| 計画時の想定 | 実測 |
|---|---|
| ATHOME/EHEYA/NIFTY/APAMAN/SMOCCA は Playwright 必須 | **5サイトともHTTPで取得できる。** v1 がブラウザを使っていたのは検索フォームを操作していたためで、URLを直接組み立てる v2 では不要（→ ADR 0010） |
| EHEYA・SMOCCA の市区値は不明（マスタが矛盾） | **どちらもJIS5桁**。東京23区ブロックが正しく、スラグのブロックが誤りだった |
| ATHOME・NIFTY の市区値はJIS5桁（マスタの記載） | **どちらもサイト固有スラグ**でマスタが誤り。実測して `08_city_site_values_slugs.sql` へ |
| APAMAN の市区値はJIS5桁 | **JIS5桁の下3桁**（新宿区 13104 → `104`）。JISから導出できる |
| 市区必須は ABLE/GOO/賃貸EX/SMOCCA | **NIFTY・APAMAN も必須**。逆に ATHOME・EHEYA は都道府県ページでも掲載が返る |
| HOME'S は AWS WAF に阻まれている（課題#17） | **回復していた**（988KB・賃料表記257件）。観測は継続 |

#### 実装内容

| 区分 | 内容 |
|---|---|
| アダプタ | ATHOME / EHEYA / NIFTY / APAMAN / SMOCCA を追加（計10サイト）。いずれもHTTP取得 |
| 依存 | `playwright` を `pyproject.toml` から**撤去**（ブラウザ約115MBのインストールが不要になった） |
| robots例外 | `SiteFetcher.ignore_robots` を新設。**APAMAN だけ**が宣言する（→ ADR 0011）。取得間隔・上限・バックオフは緩めない |
| ボット検知 | ATHOME の認証ページを**例外にする**（200 のまま返るため、黙って0件になると気づけない） |
| マスタ訂正 | `m_sites.fetch_method` を全12行 HTTP へ。`m_city_site_values` から誤った ATHOME/NIFTY 行を除き、実測値902行を `08_city_site_values_slugs.sql` として追加（計1833行） |
| テスト | 274件 → **364件**。`tests/fixtures/{athome,eheya,nifty,apaman,smocca}/` の実HTMLで回帰テスト |

#### 実測値（足立区・1ページ／MUSTを緩めて詳細取得数を確保）

| サイト | 物件 | 詳細済 | 設備平均 | 最小 | 最大 |
|---|---:|---:|---:|---:|---:|
| SMOCCA | 88 | 40 | 25.4 | 16 | 36 |
| NIFTY | 32 | 32 | 19.7 | 8 | 30 |
| APAMAN | 28 | 28 | 18.4 | 7 | 26 |
| EHEYA | 53 | 40 | 13.3 | 7 | 26 |
| ATHOME | 0 | 0 | — | — | — |

数値カラムの非NULL率は全サイト100%。`built_on`・`raw_features_text` が
EHEYA 75% / SMOCCA 45% なのは**詳細取得の上限（1実行40件）**によるもので、
残りは次回実行のキューに残る。

#### Phase 3 で判明した想定外のこと

| 事象 | 対応 |
|---|---|
| **ATHOME にパズル認証のボット検知があった** | 47件を3秒間隔で連続取得したら発動。突破せず、間隔を6秒へ広げ、認証ページを例外にした（課題#20）。現在も発動中 |
| **ATHOME の市区スラグが東京都しか集まらなかった** | 上記の発動で中断。`requires_city=False` なので実運用は止まらない（課題#21） |
| **MINIMINI は素のブラウザでも reCAPTCHA を通れない** | ユーザー判断で試したが、タイトルが「ブラウザをチェックしています」で掲載0件。偽装は行わず打ち切り（課題#18） |
| **SMOCCA は robots.txt がページ送りを禁止** | 1ページ目90件のみ取得。賃料上限も渡せない（課題#22） |
| **NIFTY は他社掲載を集約するポータル** | 外部ドメインへ飛ぶ掲載は取り込まない。そのため件数で最終ページを判定できない |
| **SMOCCA の一覧レイアウトが2種類ある** | グループ表示は賃料・敷礼が1セルに同居する。専用経路で読む |
| **APAMAN のヘッダ行が本文より1列多い** | 列位置ではなく `td.chinryo` を起点に相対位置で読む |
| **EHEYA は CSS Modules のハッシュ名でDOMが不安定** | `__NEXT_DATA__` のJSONを正典にした。無ければ例外 |

**Phase 4 への引き継ぎ**: 名寄せの検証データが揃った。同一住戸が
ATHOME 内で3件（取り扱い店舗違い）、NIFTY は SUUMO・HOME'S の掲載を集約しており
**クロスサイト重複が構造的に多い**。課題#13（SUUMO内の重複）と合わせて Phase 4 で扱う。


### 16.6 Phase 4 クロスサイト名寄せ（2026-09-02 完了）

**完了条件「ランキングがグループ単位で出る」を達成**（301掲載 → 253グループ・427テスト緑）。

#### スキーマ変更は不要だった

Phase 0 のDDLが先回りしており、`t_property_groups`・`t_properties.dedup_key` /
`group_id` / `address_normalized`・`t_notifications.group_id`・
`m_sites.representative_priority` が**すべて既に存在**した。
Phase 4 のマイグレーションは1本も追加していない
（列追加＝テーブル再作成というDB規約上のコストを丸ごと回避できた）。

#### 着手前の実測で確定させた設計（→ ADR 0012）

再設計計画 §6 は「正規化住所＋間取り＋面積＋階数」と定めていたが、
**住所の粒度と面積の丸め幅が未確定**だった。実データ301掲載で測って決めた。

| 論点 | 決定 | 実測の根拠 |
|---|---|---|
| 住所の粒度 | **丁目まで切り詰め** | HOME'S は番地・SUUMO は丁目すら出さない。切り詰めないと原理的に一致しない |
| 面積 | **丸めない**（小数第2位） | クロスサイト一致5件は全て小数第2位まで一致。丸めても一致は増えず、隣接住戸を余分に潰すだけ |
| 築年月・総階数 | **キーに入れない** | 入れると真の一致2件が分断され 5→4 に減る |
| 建物名 | **キーに入れない** | 匿名掲載（`ＪＲ相模線 上溝駅 2階建 築41年`）が実在。入れると全5件が分断される |
| 欠損時 | **キーを作らない**（NULL） | センチネル補完は「階数不明の同仕様住戸が全部潰れる」偽陽性を生む |

**偽陽性は0件**だった。グループ内で建物名が食い違う5グループを目視した結果、
食い違いの正体はすべて「片方が匿名掲載」で、いずれも真の一致だった。

#### 実装内容

| 区分 | 内容 |
|---|---|
| 名寄せ | `src/house_search/dedup/`（`address.py` / `key.py` / `groups.py`）。前2層はDBに触らない純関数 |
| キー計算 | `refresh_dedup_keys` を**一覧の upsert 直後と詳細の保存後の双方**で呼ぶ（階数・住所は詳細で埋まる） |
| グループ同期 | `sync_groups` は差分管理をせず**全体を冪等な集合演算として作り直す**。代表の交代は戻り値の `GroupChange` で受け取る |
| 代表選定 | 月額（`rent_total`）最安 → 設備抽出数 → `representative_priority` → `property_id` |
| 採点 | `t_property_scores` は物件単位のまま。設備は**グループ内の和集合**、順位は**代表と未グループにだけ**振る |
| 通知 | `new`・価格変動をグループ単位で抑制。`cheaper_listing` を実装（金額まで見て重複を避ける） |
| CLI | `regroup`（全件バックフィル・**通知なし**）と `dedup-stats`（実測）を追加 |
| テスト | 364件 → **427件**。住所正規化25件・キー20件はDB不要、グループ同期17件は `DATABASE_TEST_URL` 設定時のみ |
| ドキュメント | ADR 0012・詳細設計書 `04_名寄せ設計.md` を追加 |

#### 実測値（2026-09-02・301掲載）

| サイト | 掲載 | 代表 | 他サイトにも掲載あり | ユニーク率 |
|---|---:|---:|---:|---:|
| スモッカ | 88 | 88 | 2 | 98% |
| SUUMO | 60 | **33** | 4 | 93% |
| いい部屋ネット | 53 | 38 | 2 | 96% |
| ニフティ | 32 | 32 | 1 | 97% |
| APAMAN | 28 | 25 | 0 | 100% |
| goo不動産 | 17 | 15 | 3 | 82% |
| 賃貸EX | 11 | 10 | 1 | 91% |
| ABLE / HOME'S | 各6 | 各6 | 0 | 100% |

**重複の主因はクロスサイトではなくサイト内だった。** SUUMO は60掲載中27件（45%）が
重複で、これが課題#13 の実態である。クロスサイト重複は13掲載にとどまる。

ランキングは Phase 1 で「上位10件のうち6枠が3物件の重複」だったが、
Phase 4 適用後の上位15件では**6グループが計18掲載を6枠に集約**している。

#### あわせて処理したユーザー判断

| 課題 | 決定 |
|---|---|
| #13 同一仕様の別住戸 | **潰す＋件数を明示**。グループに全掲載を保持し、通知に「同一条件の掲載n件」と全サイトを出す |
| #18 ミニミニ | **`is_active=false`**。取得手段が無いと確定したため通常の scan から外す（名指しすれば観測できる） |
| #19 賃貸EX の説明文 | **未知表記の収集だけ外す**。設備の照合には残すので抽出数12.9件は保たれる |

#### Phase 4 で判明した想定外のこと

| 事象 | 対応 |
|---|---|
| **住所の粒度がサイト間で3段階にばらついた** | 丁目までで打ち切る以外に成立しない（→ ADR 0012 決定1） |
| **築年月・総階数を足すと名寄せが悪化した** | キーに入れない。偽陰性の増加が偽陽性の減少を上回る |
| **重複の主因はサイト内だった** | クロスサイト13掲載に対しサイト内は SUUMO だけで27掲載 |
| **賃貸EX と スモッカが同じ `external_id` を返す掲載があった** | 名寄せで1グループになるので実害はない。共通の掲載基盤を使っている可能性 |
| **再抽出では賃貸EX の生成文を分離し直せない** | `scan` は `unknown_token_text` で分けられるが `re-extract` は保存済み原文しか無い。アダプタが `mine_unknown_tokens = False` を宣言して再抽出時の収集だけ止める |

**Phase 5 への引き継ぎ**: 初回全件スキャン・タスクスケジューラ4本・pg_dump が残っている。
賃貸EX のユニーク率91%はサンプル11掲載での値なので、全件スキャン後に測り直して
本採用を判断する（→ 課題#5）。`regroup` は通知を送らないので、
全件スキャン後に1度流してから `rescore` するとよい。


### 16.7 Phase 5 賃貸本運用（2026-09-02・実装完了／実行待ち）

**完了条件「賃貸が旧システムを完全代替」は未達。** 運用スクリプト4本・バックアップ・
`--detail-limit` は完成して実測で検証済みだが、**初回全件スキャンの実行**（約6.5〜9時間・
ユーザーが起動タイミングを決める）と**タスク登録**（管理者権限が要る → 課題#23）が残っている。
437テスト緑。

#### 着手前の実測で計画の前提が2つ覆った

| 計画時の想定 | 実測 |
|---|---|
| 毎時スキャン（§9.4） | **増分スキャンは約72分かかり毎時に収まらない。** 一覧1116リクエスト＋詳細320リクエストをサイト**直列**で回すため（計画は「サイト間並列」を前提にしていたが実装は直列）。→ 2時間ごと・01:15起点に変更 |
| 初回スキャンの詳細上限は大きめでよい | **詳細2000件/サイトだと詳細だけで約12.75時間**（一覧4時間と合わせて約17時間）。800件/サイトに下げ、2晩目以降は `-Drain` モードで掃く設計にした |

**さらに重要な制約が判明した**: **レート制御は `SiteFetcher` のプロセス内にしかない。**
別プロセスの `scan` と `check-sold`、あるいは初回スキャンと定期スキャンが並走すると、
同一サイトへの実効間隔が半分になる。この1点が
①トリガーを01:15起点（奇数時+15分）にする理由 ②取得を伴うタスクを無効状態で登録する理由
の両方になっている。

#### 実装内容

| 区分 | 内容 |
|---|---|
| CLI | `scan --detail-limit`（サイトあたりの詳細取得上限を上書き）。`--full` を回し直すと一覧1100リクエストを毎回取り直すことになるので、一覧を取らずに詳細キューだけを掃く逃げ道として足した。`m_sites.daily_request_cap` が全サイト NULL で日次の安全弁が無いため「上限なし」は用意しない |
| 初回スキャン | `scripts/run_initial_scan.ps1`。ランチャー（`Start-Process` で切り離し・stdout/stderr を**別ファイル**へ）とワーカーを1本にまとめ、`-Worker` で切り替える。`-Drain` で2晩目以降の掃き出し |
| タスク実体 | `scripts/task_runner.ps1`。`-Wait` でタスクの子として待たせ、子の終了コードをそのまま返す（タスクの「前回の結果」が唯一の異常検知経路）。`task_*.log` は30日で掃除 |
| バックアップ | `scripts/backup_db.ps1`（→ 課題#8）。`pg_dump -Fc` → **`pg_restore --list` で無傷を検証** → 14世代管理。実測 334KB / `TABLE DATA` 18件 |
| タスク登録 | `scripts/register_tasks.ps1`。XMLを**UTF-16**で書き出して `schtasks /create /XML`。`-DryRun` / `-EnableScraping` / `-Unregister` を持つ |
| サイト | ATHOME を `is_active=false`（ユーザー判断 → 課題#20）。HOME'S は回復を実測済みなので有効のまま |
| 設定の整理 | `Settings.log_dir` を削除。宣言だけで src のどこからも参照されておらず「`LOG_DIR` を変えても何も変わらない」状態だった |
| 後始末 | `configs/_test/` の v1 テスト設定5本を削除（失効済みの Webhook URL を含む → 課題#14 の裏取り） |
| テスト | 427件 → **437件**。`tests/test_cli.py` を新設（引数解釈と詳細上限の決定表） |

#### タスク構成（確定）

| タスク名 | トリガー | 実行 | 実行時間上限 | 初期状態 |
|---|---|---|---|---|
| `HouseSearch-Scan` | 2時間ごと・01:15起点 | `-Task scan` | PT1H50M | **無効** |
| `HouseSearch-CheckSold` | 毎日 09:00 | `-Task check-sold` | PT1H | **無効** |
| `HouseSearch-Digest` | 毎日 20:00 | `-Task digest` | PT30M | 有効 |
| `HouseSearch-Backup` | 毎日 03:30 | `-Task backup` | PT30M | 有効 |

取得を伴う2本を無効で登録するのは、初回全件スキャンと並走させないため。
完了後に `register_tasks.ps1 -EnableScraping` で有効化する。
アイドル条件は付けない（付けると手動の `schtasks /run` でも `Queued` のまま走らない）。

#### Phase 5 で判明した想定外のこと

| 事象 | 対応 |
|---|---|
| **S4U のタスク登録が「アクセスが拒否されました」で失敗した** | 通常アカウント `wy469` のトークンに `BUILTIN\Administrators` が**まったく含まれていない**（UACフィルタ済み管理者なら Deny 付きで現れる）＝標準ユーザー。昇格では解決せず別の管理者アカウントが要る。`InteractiveToken` なら登録できることは検証用タスクで実証した（→ 課題#23） |
| **増分スキャンが毎時に収まらない** | サイト直列のため約72分。2時間ごとへ変更（→ requirements.md §4.4） |
| **レート制御がプロセス内にしかない** | 並走を設計で避ける。トリガー時刻の分離と、取得タスクの無効状態での登録 |
| **`Settings.log_dir` が未配線だった** | 削除。`.env` の `LOG_DIR` も廃止し、ログのファイル出力は `scripts/*.ps1` のリダイレクト（`logs/` 固定）に一本化 |
| **`configs/_test/` に失効済みWebhookが5本残っていた** | 現行 `.env` とハッシュ照合して失効を確認のうえ削除（→ 課題#14） |

#### 残作業（この順で行う）

1. `.\scripts\run_initial_scan.ps1` を**夜間に**起動する（約6.5〜9時間・切り離されるので端末は閉じてよい）
2. 完了後: `regroup` → `rescore` → `dedup-stats` → `coverage` → `report-unknown`
3. 詳細キューの残数を確認し、残っていれば `.\scripts\run_initial_scan.ps1 -Drain` を再実行
4. 賃貸EXのユニーク率を測り直して本採用を判断（→ 課題#5）
5. **管理者権限のある PowerShell** から `.\scripts\register_tasks.ps1`（→ 課題#23）
6. `.\scripts\register_tasks.ps1 -EnableScraping` で取得タスクを有効化
7. `schtasks /run` で4本の疎通確認 → 20:00 のダイジェストが Discord に届けば完了条件を満たす


### 16.8 Phase 5 再設計: エリア帯の導入（2026-09-02）

初回全件スキャンは完走した（02:33〜08:16・5時間43分・8,429掲載）。
**だが届いたランキングが使い物にならなかった。** Phase 5 の完了条件
「賃貸が旧システムを完全代替」は動くことしか見ておらず、役に立つことを
見ていなかった。

#### 実測で分かったこと

| 範囲 | 埼玉 | 千葉 | 神奈川 | 東京 |
|---|---:|---:|---:|---:|
| 母集団（MUST通過8,158件） | 3,202 | 1,954 | 1,500 | **591** |
| 上位100件 | 80 | 19 | 1 | **0** |
| 上位15件（ダイジェスト） | 12 | 3 | 0 | **0** |

上位100件の市区は千葉県茂原市11・埼玉県本庄市9・毛呂山町7・上里町7…と、
**群馬/栃木県境の埼玉県北西部と房総半島**だった。パターン名は
「東京賃貸一人暮らし」である（→ 課題#24）。

⚠ **スコア自体は高い**（上位は66〜72点）。数値だけを完了条件にしていると
正常に見えてしまう。

#### 決定（→ ADR 0013・CONTEXT.md）

| # | 決定 |
|---|---|
| 1 | **エリア帯**を導入し1パターン＝1帯にする。帯は**行政区画では切れない**ので通勤の許容時間で市区を選ぶ |
| 2 | 帯1＝東京23区（23市区）。MUST 12万円は**暫定**（7万では30件しか無い）。広く取って `rescore` で締める |
| 3 | 帯2＝近郊45分圏（63市区）。MUST 据え置き（実測で通過1,128件・代表920件） |
| 4 | 帯どうしは重ならない（回帰テストで固定） |
| 5 | **採点も帯に閉じる**。取得URLを絞るだけでは帯外の既存データに帯のスコアが付く |
| 6 | **新着監視**（増分・2時間ごと）と**在庫棚卸し**（`sweep`・週1日曜02:00）を分ける |
| 7 | `check_sold` の対象を「そのパターンで採点されている掲載」に限る |
| 8 | 取得を伴う処理を **pg_advisory_lock** で排他する（時刻の分離という約束に頼らない） |
| 9 | 非リトライ4xxは404とそれ以外で分ける（404は返す／拒否系は数えて5回で打ち切る） |
| 10 | 賃貸EX を本採用（45分圏でユニーク率63%・69代表） |
| 11 | 完了条件を**成果物の質**（上位15件が内見したい物件か）で判定する |

#### あわせて見つかった不具合

| 事象 | 対応 |
|---|---|
| **非リトライ4xxで「連続失敗による打ち切り」が発火しない。** NIFTY の405を271回叩き続けた（エラー555行中542行） | 失敗として数える。実地で HOMES が5回で打ち切られることを確認（→ 課題#25） |
| **404 による掲載終了の検知がデッドコードだった。** `raise_for_status()` が先に例外を投げ `is_sold` の404判定に到達しない | 404 は `response` を返す（→ 課題#25） |
| **SUUMO の `ct` に端数を渡すと HTTP 200 のまま0件。** `ct=15.6` で0件・`ct=16.0` で100件 | 整数の万円へ切り上げ。HOME'S も0.5刻みへ（→ 課題#29）。⚠ **検出手段は未整備** |
| **NIFTY の405は一時的だった。** 既定UA・Referer無しでも200で取得できる | 405 自体ではなく、拒否されたときに手を引けないことが問題だった |
| **賃貸EXの `max_pages_per_run=50`。** 86市区×50ページで賃貸EXだけ3.6時間 | 他サイトと同じ5へ |
| **`digest_group` が「見出しラベル」だけの実装。** 本書と要件定義書は「セクション並記」と書いている | ドキュメント側を実態に合わせるか判断待ち（→ 課題#28） |
| **古いパターンのスコア行が残る。** `Phase3検証` が301件 | 掃除の仕組みが要る（→ 課題#27） |

#### 副次的な効果

- 対象市区が **216 → 86** に減り、増分スキャンが約72分 → **約53分**に短縮する。
  空いた時間に `check-sold`・`digest`・`backup` を並走させずに置ける
- `cities` を明示すると `requires_city=False` のサイトも市区展開されるため、
  **SUUMO・HOMES・EHEYA の母集団が増える**。SUUMO は一覧で2,248件しか見ておらず、
  APAMAN（37,163件）の**1/17**だった

#### 実施した検証（2026-09-02）

帯1＋帯2 のシードスキャンは 10:35〜15:51（5時間16分）で完走した。新規1,549掲載。
エラーは42行で前回555行から激減している（NIFTY の405が542件→16件。
**打ち切り修正が実地で効いた** → 課題#25）。

**SUUMO の母集団が市区展開で8.1倍になった**（一覧 2,248→18,209件・新規 155→485件）。
`requires_city=False` のサイトが都道府県4本しか叩いていなかった問題が解けている。

`regroup` → `rescore` で**エリア帯が採点に効いている**ことを確認した
（帯1 649件・帯2 2,178件しか採点されず、帯外の約6,800件は対象外）。
ランキングの中身も意図通りになった（→ 課題#24 の実測表）。

途中で見つけて直したもの: 市区名の表記ゆれ（鎌ヶ谷/鎌ケ谷）による city_id の欠落と
帯の二重採点（93件）、対象外スコア行の残留（→ 課題#27）、用語統一のリネーム（→ 課題#30）。

#### 残作業（この順で行う）

1. **SUUMO と HOMES の取り直し**（`ct` の修正前に走ったため0件だった）
2. `regroup` → `rescore` → `dedup-stats` → `coverage` → `report-unknown`
3. **帯1の MUST 12万円を実測で締める**（賃料×面積の分布を見る）
4. **管理者権限のある PowerShell** から `.\scripts\register_tasks.ps1`（→ 課題#23）
5. `.\scripts\register_tasks.ps1 -EnableScraping` で取得タスク3本を有効化
6. 20:00 のダイジェストを2通受け取り、**上位15件が実際に内見したい物件か**を見る


### 16.9 Phase 5A マスタ全国化（2026-09-03 完了）

**完了条件「全国どの市区でも検索URLを組み立てられる」を達成**（1,918行・47都道府県・
`jis_code` 100%・476テスト緑）。→ [ADR 0014](./adr/0014-nationwide-city-master.md)・課題#16

#### 着手前の実測で、想定より深刻な壊れ方が見つかった

計画では「JISコードの欠落を埋める」つもりだった。実測すると**誤りのほうが問題**だった。

| 事象 | 実測 |
|---|---|
| マスタの範囲 | 947行・**15都道府県**（47中）。`jis_code` は 631/947 |
| **他市のコードの混入** | 名古屋市に**北名古屋市**の `23234`、大阪市に**東大阪市**の `27227` |
| **同じコードを持つ行が2つある** | 上記2組。JIS系サイトでどちらを指定しても別の市の一覧が返る |
| 区再編への未追随 | 浜松市の3区が2024年の再編（7区→3区）前のコードのまま |

⚠ **この壊れ方の質の悪さ**: 誤ったコードでも**取得は成功し、掲載も返り、
エラーにもならない**。ただ別の市の物件が入るだけである。エリア帯が
東京・千葉・埼玉・神奈川に閉じていたから表面化しなかっただけで、
愛知・大阪へ広げていたら気づけないまま誤ったデータが積もっていた。

原因は Phase 2 で**サイトのエリア索引から部分文字列一致で補完した**こと。
行政区画のコードは政府が配っているのだから、サイトから推定するのが誤りだった。

#### 実装内容

| 区分 | 内容 |
|---|---|
| 原典 | `data/city_master/soumu_local_gov_codes.csv`（総務省・政府標準利用規約・基準日2024-01-01）を Git 管理下に置く。Excel はバイナリで差分が読めず配布URLも改訂で変わるため、取得済みCSVを版として残す |
| 生成器 | `scripts/tools/generate_city_seed.py`。`--fetch` で原典を取り直し、既定でシードSQLを生成、`--report` でDBとの差分だけを出す |
| シード | `db/seed/06_cities.sql` を**全国版で置き換えた**（1,918行）。2つのファイルが `m_cities` を所有する混乱を避けるため、追加ではなく置換にした。既存947行が総務省表記と**一文字違わず一致**したので単純な upsert で済んだ |
| マイグレーション | `d7b3e9c41a58`。誤コード5件を訂正してから `uq_m_cities_jis_code`（部分ユニーク索引）を張る。**訂正が先でないと索引の作成そのものが失敗する** |
| エリア展開 | `scrape/area.py` で**行政区を持つ政令市の親行を外す**。`search.cities` に市名だけ書いたらその行政区へ展開する |
| 市区解決 | `CityIndex` を新設。全国化で全国一意な市区名が 1,861/1,918 に減るため、都道府県を前置しない住所は**検索パターンの対象都道府県の中でだけ**一意性を判定する |
| バックフィル | CLI `resolve-cities`（ネットワーク不要）。マスタ入替後に既存掲載の `city_id` を引き直す |
| テスト | **476件**（今回23件を追加）。`tests/test_city_master.py` を新設（原典の整合・誤コード5件の回帰・生成SQLとCSVの同期） |

#### 実測値

| 指標 | 適用前 | 適用後 |
|---|---:|---:|
| `m_cities` 行数 | 947 | **1,918** |
| 都道府県 | 15 | **47** |
| `jis_code` 非NULL | 631 | **1,918（100%）** |
| 既存掲載の `city_id` 解決率 | 10,312 / 10,322 | **10,322 / 10,322（100%）** |
| 両方の帯で採点される掲載 | 0 | **0**（不変） |
| 上位10件の市区 | 帯内 | **帯内**（不変） |

内訳は市区町村1,747（一般に言う1,718市町村＋東京23区＋北方領土6村）＋
政令指定都市の行政区171。

#### Phase 5A で判明した想定外のこと

| 事象 | 対応 |
|---|---|
| **他市のコードが5件混入していた**（欠落ではなく誤り） | 名前で照合してコードを上書きする方式にし、部分ユニーク索引でDB側が弾くようにした |
| 全国で唯一、同一都道府県内で市区町村名が衝突する組がある | 北海道の泊村（後志 `01403` / 根室 `01696`）。政令市の区を市名で prefix するのと同じ考え方で、北方領土側を `国後郡泊村` として区別 |
| 既存947行の表記が総務省と**完全一致**していた | 表記ゆれの吸収機構は不要だった。`canonical_name` を変えない規則だけ残す |
| **全国化で市区解決が劣化したのは1件だけ**（`府中市白糸台２`） | 逆に新たに10件解決できるので差し引きプラス。ただしエリアを広げるほど効く問題なので `CityIndex` で範囲を絞る仕組みを入れた |
| 過去の「鎌ヶ谷市／鎌ケ谷市」事故はマスタの問題ではなかった | マスタもYAMLも総務省表記で正しく、**サイトが返す住所**が小書きだった。直す場所は解決側 |

#### 残作業

- **サイト固有スラグは全国化していない**（HOMES 158 / ATHOME 78 / NIFTY 824 /
  MINIMINI 158行）。JISから導出できず各サイトのエリア索引から集めるしかない。
  ATHOME はボット検知が発動中で収集できない（→ 課題#20・#21）


### 16.10 Phase 5B サイト側MUST（2026-09-03 完了）

**完了条件のうち SUUMO について達成した**（母集団57%削減・不等号の向きを実測で確認・
510テスト緑）。のち **HOMES（PR #17）・GOO（PR #21）を配線した。APAMAN と検出の残り2層が未着手。**
→ [ADR 0015](./adr/0015-site-side-must-filters.md)・課題#29

#### ADR 0003 は撤回せず「補強」した

ADR 0003（サイト側フィルタ全廃）の論拠は **WANT についての議論**だった。
WANT は「満たさなくても対象から外れない」条件なので、サイト側で落とすと定義が壊れる。

**MUST は違う。** MUST を満たさない掲載はローカルでも `fail` になり
DBにも入らず採点もされない。サイト側で落としても結果は1件も変わらず、
変わるのは取得量だけである。この一点が本Phaseの全体を支えている。

⚠ **設備条件（`must.features`）だけは永久に送らない。** 辞書抽出とサイトのタグ付けは
判定が食い違い、こちらが `unknown` として残したい掲載をサイトが落としてしまう。

#### 実測でキーと選択肢を確定させた（推測で書かない）

資料 `資料_サイト別検索条件一覧.md` には SUUMO 賃貸の**フォームの選択肢**は
載っているが、**URLのクエリキーは載っていない**。推測で書くと課題#29 を再発させるので、
実サイトで測って確定させた（足立区 `sc=13121`・基準 62,030件）。

⚠ **測り方に一手間かけた。** 先に存在しないキー `zzz=1` を送って件数が
62,030件のまま変わらないことを確かめ、「**件数が動く＝キーが認識されている**」と
言えるようにしてから測った。これが無いと「効いていないのに効いたつもり」になる。

| キー | 軸 | 実測 |
|---|---|---|
| `mb` / `mt` | 専有面積 下限/上限 | 20〜100㎡・5刻み。`mb=30` で 31,861件 |
| `et` | 駅徒歩 | 1/5/7/10/15/20分。`et=10` で 41,405件 |
| `cn` | 築年数 | 1/3/5/7/10/15/20/25/30年。`cn=10` で 43,750件 |
| `md` | 間取り | `04`=1LDK `05`=2K `06`=2DK `07`=2LDK `10`=3LDK |

`md` はいずれも **S付き（1SLDK 等）を含む上位集合**だった。余分に取るだけなので安全。

#### 実装内容

| 区分 | 内容 |
|---|---|
| 共通コード | `scrape/params.py`。軸・丸め・単位換算。**丸めの向きは `AXIS_BOUND` で軸ごとに決まり、サイト定義には書けない**（サイトごとに持たせると必ず間違える） |
| 正典 | `data/site_search_params.yaml` → `m_site_search_params`（`sync-site-params`）。設備抽出辞書と同じ構成 |
| 設定 | `search.site_filters`（`enabled` / `axes` / `exclude_sites`）。**既定は無効**で、事故時は `enabled: false` に戻せば全サイトが従来動作へ戻る |
| アダプタ | SUUMO が `supports_site_filters = True` を宣言。クエリ文字列を持たないURL体系のサイトへ機械的に付けても効かないので、対応の可否はアダプタに委ねる |
| 事故の検出 | **SUUMO のエラーページを `parse_list` が例外にする**。`scan` は1ページの失敗でサイトを止めないので、エラーとして記録され実行サマリに出る |
| テスト | 476 → **510件**（34件追加）。`tests/test_site_params.py` を新設 |

#### 実測値（2026-09-03・SUUMO 足立区）

| 指標 | 絞り込みなし | フィルタあり |
|---|---:|---:|
| 母集団 | 62,030件 | **27,150件（57%削減）** |
| 1ページ目149件中のMUST通過 | 5件 | **12件** |
| 面積が30㎡未満 / 徒歩20分超 / 間取り対象外 | — | **すべて0件** |

不等号の向きが3軸とも正しいことを実データで確認した。

#### Phase 5B で判明した想定外のこと

| 事象 | 対応 |
|---|---|
| **無効値で SUUMO はエラーページを HTTP 200 で返す**（`title` が「エラー｜SUUMO(スーモ)」の11KB） | 判別できるので `parse_list` が例外にする。ATHOME の認証ページと同じ扱い |
| **存在しないキーは黙って無視される**（`zzz=1` で件数が変わらない） | 「絞れていないことに気づけない」ので、測る前に判定方法の妥当性を担保する手順を入れた |
| autogenerate が無関係なドリフトを大量に拾った | `uq_m_cities_jis_code` の削除や制約名の逆戻しまで含んでいた。**マイグレーションは手書きにした** |
| psycopg3 は匿名複合型（タプル）の配列を渡せない | 同期の DELETE で `(code, code, axis)` の配列比較が `FeatureNotSupported` になる。文字列キーへ連結して比較する |

#### 残作業

1. ~~**APAMAN の配線**~~ → **完了（2026-09-03）**。`senyu1`（面積下限）・
   `ekitoho`（駅徒歩）を実測で確定。⚠ **総件数を出さないので返る掲載の中身で確かめた。**
   ⚠ 間取り（madori）はキーを確定できたが**送っても効かない**ため配線していない。
   ⚠ **配線の前に `page_url` の不具合を直した**（`?` を重ねると page が黙って
   無視され1ページ目を返し続ける）。`query_separator` に共通化し EHEYA・GOO も寄せた
2. ~~**対照取得**~~ → **完了（2026-09-03）**。フィルタ付きの一覧が0件のとき、
   パラメータを外した同じURLを**1回だけ**取り直す。対照も0件なら黙り、掲載があれば
   フィルタが原因として記録する。⚠ サイトごとに1回だけ・正常時は増やさない
3. ~~**連続0件のバックストップ**~~ → **完了（2026-09-03）**。そのサイトの既存掲載数と
   突き合わせ、実績があるサイトの0件を異常として申告する
4. `coverage` に一覧の取得件数を出す（**残**。実測の見やすさの話で、
   検出そのものは上の2層で足りている）


### 16.11 Phase 5C 通勤時間（2026-09-03 完了）

**完了条件「上位15件すべてに通勤時間が付き、実際に内見したい物件が並ぶ」を達成**
（566テスト緑）。→ [ADR 0016](./adr/0016-commute-time-rail-graph.md)・課題#24

#### 着手前の前提が実測で覆った

確定方針は「Google Maps Routes API（TRANSIT）で駅ペアを1回計算してDBキャッシュ」で、
APIキー・課金設定まで済ませてあった。実装して叩いたところ **HTTP 200 のまま本文が `{}`**
になり続けた。

| 呼び出し | 結果 |
|---|---|
| Routes API TRANSIT・川崎→芝公園 | `{}` |
| **Routes API TRANSIT・米国（タイムズスクエア→ウォール街）** | **経路を返す** |
| Routes API **DRIVE**・川崎→芝公園 | 経路を返す |
| レガシー Directions API | キーのAPI制限で `REQUEST_DENIED` |

同じキー・同じ座標で**米国だけ動き、日本でも DRIVE なら動く**。公式の Map coverage details にも
日本は driving / walking しか無い。駅すぱあと API はフリープランに経路探索が含まれない。

⚠ **「200 で返る失敗」の4例目**だった（SUUMO のエラーページ・ATHOME の認証ページ・
課題#29 の無効パラメータに続く）。`{}` を `no_route` として明示的に記録する設計にしていたため
**3件取得した時点で気づけた**。握りつぶしていれば「通勤時間が全件 unknown」のまま完了と
報告していた。

#### 実装内容

| 区分 | 内容 |
|---|---|
| スキーマ | `m_stations` / `t_listing_stations` / `t_station_commutes` の3テーブルを新設。**既存テーブルは無変更**（列追加＝テーブル再作成というDB規約のコストを回避） |
| 駅マスタ | 駅データ.jp 無料版が正典。`sync-stations` で同期（10,465駅 / 8,766駅グループ）。⚠ **再配布不可のためCSVはGit管理外**で、正典の差分レビューができない唯一の例 |
| 駅の同定 | 保存済みの `station_info` からの純関数（`commute/matcher.py`）。**アダプタは触らない** |
| 所要時間 | 駅間接続をグラフにしてダイクストラ。目的地からの単一始点最短路で**全国8,743グループが0.1秒** |
| 校正 | NAVITIME の実測12ペア（芝公園ゆき・水曜08:30発）で最小二乗。robots.txt の `Crawl-delay: 10` に合わせ11秒間隔で12件だけ取得 |
| 採点 | MUST `commute_minutes_max`（両帯60分）＋ WANT `commute_minutes` weight 25（徒歩を15→10に下げて捻出） |
| CLI | `sync-stations` / `resolve-stations` / `resolve-commutes` / `commute-stats` |
| テスト | 510 → **566件** |

#### 実測値

| 指標 | 実測 |
|---|---|
| 駅の同定率 | **99.4%**（10,259 / 10,322掲載）。サイト別 98.3〜100% |
| 曖昧（ambiguous） | 23件（0.2%）。都道府県で絞ると168件から減った |
| 所要時間の誤差 | 平均**5.6分**・最大16.0分（一律の表定速度だと平均18.2分・最大72分） |
| 到達不可 | 0件（1,154駅グループ） |
| 上位15件の通勤時間 | 帯1: 27〜40分 / 帯2: 41〜57分 |
| 0点張り付き | **両帯とも0件**（課題#31 の失敗を回避） |
| MUST 60分の効き | 帯1は0件除外（元から100%が60分以内）／帯2は375件を除外 |

#### Phase 5C で判明した想定外のこと

| 事象 | 対応 |
|---|---|
| **Google Maps が日本の公共交通を返さない** | 駅データ.jp の接続情報から自前計算へ（→ ADR 0016）。Routes API のコードと `GOOGLE_MAPS_API_KEY` は残さず削除した |
| **一律の表定速度では長距離が過大**（最大72分ずれ） | 距離＋乗換＋定数の回帰式に変えて平均5.6分へ |
| **NAVITIME の月指定は `2026/09` 形式**。`202609` は黙って無視される | 深夜に実行した1回目が全件始発帯の値になっていた。形式を直して取り直した |
| **駅データ.jp 無料版に新幹線の駅が1件も無い** | 本庄早稲田48件が同定できない（帯外なので実害なし。→ 課題#33） |
| **`ScanSummary.errors` が property で `append` しても消える**（silent failure） | `pattern_errors` フィールドを足して合流させた |
| **バス停名に駅名と紛らわしいものがある**（栄町・一之江五丁目） | 駅名を拾う前にバス停部分をマスクする |
| **帯2「近郊45分圏」の45分以内が16.3%しかない** | 市区リストが通勤時間の近似として不正確だった（→ 課題#32） |

#### 残作業

1. **帯2の市区リストを実データで見直す**（→ 課題#32）
2. **管理者権限のある PowerShell** から `scripts/register_tasks.ps1`（→ 課題#23）
3. `scripts/register_tasks.ps1 -EnableScraping` で取得タスクを有効化
4. Phase 5B の残り（検出2層。サイト側MUSTの配線は SUUMO・HOMES・GOO・APAMAN で完了）


### 16.12 Phase 5D 通勤時間の実ダイヤ化（2026-09-03 完了）

**完了条件「上位15件の通勤時間が実ダイヤの値になる」を達成**
（全1,155駅を4時間48分で取得し、上位15件は帯1で13件・帯2で14件が実ダイヤ）。
591テスト緑。→ [ADR 0017](./adr/0017-navitime-real-timetable.md)・課題#34

#### ODPT を使わないと決めた

第0歩として ODPT（公共交通オープンデータセンター）のカバー範囲を実測し、
スパイク一式まで用意してあった。それでも使わない判断にした。

| 論点 | 実測・判断 |
|---|---|
| 登録 | 開発者登録とトークン発行に日数がかかり、**着手を待たせる** |
| カバー範囲 | 列車時刻表10社・駅時刻表のみ5社。**京成・北総・東葉高速・埼玉高速・東京モノレールは未参加** |
| 再配布 | 基本ライセンスが生データの再配布を制限し、原典を Git 管理下に置けない |

⚠ **穴が残るのが致命的だった。** 時刻表のある区間と無い区間が混ざると、
1つの経路の中で「実ダイヤの区間」と「回帰式の区間」を足すことになり、
どちらの精度でもない値が出る。**しかも結果を見てもどちらだったか分からない。**

NAVITIME の乗換案内は登録不要で、優等列車・直通・乗換待ちを織り込んだ結果を
そのまま返す。スパイク一式は将来の一括生成に備えて残した。

#### 設計を1つ変えた（区間の集積 → O-D の直接取得）

前セッションの設計は「列車の連続停車ペアを全部辺にしてダイクストラ」
（スキップ辺方式）だったが、**必要なのは1,154駅グループ → 目的地1つ**である。
O-D を直接引けば取得1,154回で厳密な答えが出る。

⚠ **乗換案内で隣接駅を引くと待ち時間が混ざる。** 2駅ぶんの検索結果は
「次の列車を待つ時間＋乗車時間」なので、区間を足し上げると大きく過大になる。
スキップ辺方式そのものは残っており、**O-D の経路明細から乗車区間を副産物として貯める**
（`t_rail_segments`）。目的地が変わったらこの辺で計算し直せば取得のやり直しが要らない。

#### 実装内容

| 区分 | 内容 |
|---|---|
| スキーマ | `t_navitime_routes`（経路の原文）・`t_rail_segments`（乗車区間の実所要時間）の2テーブル。**既存テーブルは無変更** |
| 取得・解析 | `commute/navitime.py`（純関数のパーサ＋URL組み立て）。取得は既存の `SiteFetcher` を再利用し robots.txt・間隔・打ち切りをそのまま使う |
| 保存 | `commute/timetable.py`。1駅ごとにコミットし、取得済みは次回スキップ |
| CLI | `fetch-commutes`（`--pattern` / `--limit` / `--station` / `--refetch` / `--depart-on`） |
| 運用 | `scripts/run_fetch_commutes.ps1`（`Start-Process` で切り離し・stdout/stderr は別ファイル） |
| 保護 | `resolve-commutes`（回帰式）は `source='navitime'` の駅を対象から外す |
| テスト | 566 → **583件**。`tests/fixtures/navitime/` の実HTMLで回帰テスト |

#### 実測値（2026-09-03・8駅）

| 駅 | 実ダイヤ | 回帰式 | 差 | 乗換 |
|---|---:|---:|---:|---:|
| 目黒 | 10分 | 14分 | +4分 | 0回 |
| 恵比寿 | 16分 | 21分 | +5分 | 1回 |
| 渋谷 | 18分 | 23分 | +5分 | 1回 |
| **川崎** | **28分** | **43分** | **+15分** | 2回 |
| 鹿島田 | 38分 | 39分 | +1分 | 2回 |
| 矢向 | 39分 | 41分 | +2分 | 3回 |
| 平間 | 40分 | 38分 | -2分 | 1回 |
| 横浜 | 41分 | 49分 | +8分 | 2回 |

平均5.2分・最大15分。⚠ **ずれの向きが問題だった。** 回帰式は優等列車を
表現できないぶん**過大**に出るので、川崎のように速い列車が停まる駅が
15分ぶん不当に沈んでいた。乗車区間は8駅から36本（列車34・徒歩2）貯まっている。

#### Phase 5D で判明した想定外のこと

| 事象 | 対応 |
|---|---|
| **NAVITIME は自己申告のUAを 403 で拒否する** | robots.txt は `/transfer/` を許可しており、UAの選別だけが別の関門。HOME'S と同じ扱いでブラウザ相当UAにした |
| **同名異駅を黙って別の駅で検索する**（`大久保` → `大久保（東京都）`・HTTP 200） | `駅名（都道府県名）` で厳密指定し、**応答が解決した駅名を照合してから保存する** |
| **「発」から「着」までが1区間とは限らない**（`（直通）東京` で列車が変わる） | 直通の地点で区間を切る。1区間として読むと辺の重みが5分短くなる |
| **並び順の1本目が最短とは限らない**（実測で rank 1 が43分・別候補が39分） | 候補の中から最短を採る |
| **`ON CONFLICT` は同一コマンド内で同じ行を2度更新できない** | 保存前に区間キーで畳む（`merge_observations`） |

#### 全件取得と採点への反映（2026-09-03）

`scripts/run_fetch_commutes.ps1` が 04:42〜09:30（**4時間48分**）で完走した。

| 指標 | 実測 |
|---|---|
| `t_station_commutes` | navitime **1,126駅** / rail_graph 29駅 |
| `t_rail_segments` | 2,759本（列車2,729・徒歩30） |
| 保存しなかった駅 | 29件。**NAVITIME が意図と違う駅として解決した**もので、照合が正しく弾いた |

**母集団が動いたので `best`/`worst` を付け直した**（→ 課題#31 と同じ手順）。
⚠ **これが本Phaseで最も見落としやすい点だった。** 算出方法を差し替えると
分布そのものが動くので、回帰式向けに決めた値は実ダイヤでは合わない。

| 帯 | 母集団（MUST通過） | 回帰式のとき | 実ダイヤ後 | 据え置いた場合 |
|---|---|---|---|---|
| 東京23区賃貸 | 274件・12〜60分・中央39分 | 20〜45 | **25〜55** | 0点が**24.8%**に張り付く |
| 近郊60分圏賃貸 | 1,574件・25〜60分・中央53分 | 35〜60 | **40〜60** | 満点が1.5%しか出ず上端が死ぬ |

**上位15件は実ダイヤで並び替わった。** 帯1の1位は足立区の1LDK（通勤33分・
徒歩17分・78,300円・40.4㎡・築21年）で、上位は足立・葛飾・江戸川・荒川に散っている。
帯2の1位は鎌ケ谷市の2DK（通勤55分・49,000円・37.9㎡）。
⚠ **上位15件のうち3件（帯1の3位・15位＝町屋、帯2の11位＝本八幡）は回帰式のまま**で、
これは下記の29駅に当たる。

**あわせて帯2を実態へ合わせた**（→ 課題#32）。実ダイヤで測ると45分以内は9.7%しか
無かったため「近郊45分圏」→**「近郊60分圏」へ改称**し、
**通勤60分以内の掲載が1件も無い4市区**（ふじみ野市88件・東村山市88件・国立市19件・
清瀬市90件＝計261掲載）を外して 63→**59市区**にした。取っても MUST で全件落ちるため
取得が丸ごと無駄になる。⚠ **市区を減らす方向は取り直しにならない**が、
増やす方向は MUST 1段目で fail した掲載がDBに残っていないため取り直しになる。

#### 残作業（この順で行う）

1. ~~29駅の再取得~~ → **完了（2026-09-03）。芝公園ゆきは1,155駅（100%）が実ダイヤ**。
   ⚠ **原因はこちらの指定方法だった。** 都道府県を添えると同名駅の無い駅で
   完全一致に失敗し、近い名前の別駅へ落ちていた（`松田（神奈川県）` → 新松田）。
   `駅名` → `駅名（都道府県）` の順に試すよう改め、20駅を救済。
   残る2駅は表記ゆれ（`三ツ沢上町` / `三ッ沢上町`）で `normalize_key` に `ッ→ツ` を追加
2. ~~乗車区間が地方外で貯まらない~~ → **完了（2026-09-03・課題#35）**。
   索引を `--region` の地方に合わせ、駅名の注記（`本八幡〔新宿線〕`・
   `押上[スカイツリー前]`）を落とすようにした。**`re-segment` を追加**し、
   保存済みの経路原文から区間を作り直せるようにしてある（ネットワーク不要）。
   ⚠ **関東の取得は3駅しか進んでいなかったので取り直しの損失はゼロだった。**
   実測: 沖縄 0 → 18区間・関東 7.7% → 100%・芝公園 90.1% → 94.8%
3. **全国網羅の残り7地方**（→ [ADR 0018](./adr/0018-nationwide-commute-destinations.md)）。
   関東（1,898駅・7.9時間）・北海道・東北・中部・近畿・中国・四国・九州で**約36.5時間**。
   ⚠ 1コマンドで全国を回さず地方ごとに走らせる（1駅ごとにコミットするので再実行で続く）
4. Phase 5B の残り（**APAMAN** の配線と、一覧0件時の対照取得・連続0件のバックストップ）
5. **管理者権限のある PowerShell** から `scripts/register_tasks.ps1`（→ 課題#23）
6. `scripts/register_tasks.ps1 -EnableScraping` で取得タスクを有効化


### 16.13 取得可否の実測とサイト拡張の計画（2026-09-03）

Phase 5D までで通勤時間まで揃ったので、**実装済み10サイトが想定どおり取れているか**を
実機で測り直し、あわせて未対応サイトを洗い出した。詳細は
[詳細設計書 §9](./詳細設計書/01_サイト取得設計.md)。

#### 単発の疎通確認では制限を再現できなかった

足立区の一覧1ページを取ると **10サイトすべてが HTTP 200 で正常に返る**。
しかし市区を変えながら連続で叩くと、3サイトで挙動が分かれた。

| サイト | 連続取得の耐性 | DBの掲載数 |
|---|---|---:|
| SUUMO / GOO / ABLE / CHINTAI_EX / EHEYA / APAMAN / SMOCCA | 制限を確認せず | 273〜2,166 |
| **NIFTY** | **10市区を3秒間隔で全件成功**（405 は解消） | 74 |
| **HOMES** | **10秒間隔でも6リクエスト目から HTTP 202＋空ボディ** | 10 |
| **ATHOME** | **6秒間隔で5リクエスト目からボット認証ページ** | 0 |

⚠ **「1回叩いて 200 が返った」を取得可能の根拠にしてはいけない。**
3サイトとも1リクエスト目は正常に返る。

⚠ **HOMES は間隔を広げても上限が動かなかった。** 課題#17 で 2.5 → 10.0 秒へ広げたが、
発火位置は「4秒間隔で6件目」（課題#17 の実測）と**同じ6件目**だった。
リクエスト数で絞られており、間隔を広げても**所要時間が伸びるだけ**なので 2.5秒へ戻す。

#### ローテーションの前提が足りないことも実測で分かった

市区ローテーションを設計するにあたり検索値の充足を調べたところ、
**回せる市区がそもそも足りなかった**。

| サイト | 帯82市区のうち検索値が無い市区 | 回せる市区 |
|---|---:|---:|
| HOMES | **33**（川口・草加・越谷・戸田…埼玉と千葉が中心） | 49 / 82 |
| ATHOME | **50**（さいたま市の全区・川口・草加…） | 32 / 82 |
| NIFTY | 1（府中市） | 81 / 82 |

⚠ **`resolve_areas` は検索値の無い市区を黙って落とす。** この状態でローテーションを
実装すると、**帯2の埼玉・千葉が丸ごと歯抜けのまま「一巡した」ことになる**。
ユーザー判断（2026-09-03）で、両サイトのエリア索引から収集して埋めてから着手する
（ATHOME は課題#21 の失敗を繰り返さないよう **15秒以上の間隔で3リクエストだけ**）。

#### 設計上、特に見落としやすい3点

1. **帯が2つあるので予算が2倍消費される。** HOMES は両帯の `sites:` に載っており、
   素朴に実装すると1回の `scan` で 5+5=10 リクエストが飛び、後半の帯が全部 202 になる。
   カーソルを `(サイト, パターン)` 単位で持ち、**1回の実行では `updated_at` が
   最も古い1帯だけ**を回す
2. **予算を一覧で使い切るので詳細が永久に取れない。** 初版は両サイトの詳細取得を
   0件に固定し、一覧項目だけで MUST 判定と採点に載せる（一覧に賃料・面積・間取り・
   築年・徒歩はあるので1段目は成立する）。予算の回復窓を実測してから
   「N回に1回の詳細回」を織り込むかを決める
3. **カーソルは位置番号でなく JIS5桁で持つ。** 市区リストは YAML 編集で増減する
   （課題#32 で実際に4市区を外した）ため、番号だと編集のたびにずれる。
   `resolve_areas` は `jis_code` 順で決定的に並ぶので、
   「カーソルより大きい最初の JIS から n 件、末尾に達したら先頭へ戻る」で周回する

**状態は新テーブル `t_site_scan_cursors`**（`(site_id, pattern_name)` PK・
`last_city_jis`）に置く。`m_sites` への列追加は監査カラムを末尾に保つため
テーブル再作成を伴うが、**テーブル新設はそのコストが掛からない**
（Phase 5C・5D が既存テーブル無変更のためにテーブル新設を選んだのと同じ判断）。
回転量（HOMES 5・ATHOME 4）は運用値ではなくサイトの実測特性なので、
`supports_site_filters` と同じく**アダプタのクラス属性**で宣言する。

#### 追加するサイト（ユーザー判断 2026-09-03）

robots.txt を実測して選定した（→ 課題#37・詳細設計書 §9.2）。

| サイト | 重複の少なさ | 実装コスト | Phase |
|---|---|---|---|
| **UR賃貸住宅** | 極めて高い | 中 | 5F |
| レオパレス21 / D-room | 高（自社物件） | 低 | 5G |
| ハウスコム / ホームメイト / CHINTAI.net | 中〜低 | 低 | 5H |

- **ピタットハウスは対象外**（`Disallow: /rental/` で賃貸一覧そのものが禁止）
- **JKK東京は見送り**（空室検索が別ホストのセッション型アプリで実装コストが重い）
- ⚠ **UR は掲載の粒度が他サイトと違う**（団地と住戸が別階層で、住戸単位は JSON API）。
  `SiteScraper` は GET＋HTML一覧を前提にしているため、
  任意フック `collect_listings` をアダプタが宣言したら委譲する方式で収める
  （`supports_site_filters` と同じ宣言ベースの拡張。**既存10アダプタは触らない**）
- ⚠ **UR は礼金・仲介手数料・更新料・保証人が不要**なので、`rent_total` だけで
  他サイトと比べると**初期費用の有利さがスコアにまったく現れない**。
  派生トークンで既存 WANT（礼金なし・保証人不要）へ載せる


### 16.14 Phase 5E 取得の回復（2026-09-03 完了）

**完了条件「上限を踏んでも次の市区・次の実行へ進む」を実地で確認した**
（646テスト緑・HOMES の掲載が 10 → 28件）。→ 課題#36・#17・#20・#21

#### 前提のスラグ収集で、計画になかった罠を2つ踏んだ

市区ローテーションは「82市区を順に巡る」仕組みなので、**巡る先が揃っていないと
歯抜けのまま『一巡した』ことになる**（`resolve_areas` は検索値の無い市区を
黙って落とす）。そのため実装より先に HOMES・ATHOME のエリア索引から
市区スラグを集めた（`scripts/tools/collect_city_slugs.py`）。

| サイト | 収集前（帯82市区） | 収集後 |
|---|---:|---:|
| HOMES | 50 / 82 | **82 / 82** |
| ATHOME | 32 / 82 | **81 / 82**（東京都府中市のみ残る） |

⚠ **罠1: 属性のクォートが `"` と `'` で混在していた。** ATHOME の市区リンクは
``href='/chintai/saitama/fujimino-city/list/'`` と単一引用符で、`"` 決め打ちの
正規表現だと**71市区のうち10市区しか拾えない**。
⚠ **エラーにならず件数が減るだけ**なので、リンクの総数を数えて初めて気づいた。

⚠ **罠2: これに気づけたのは取得HTMLを保存していたからである。** `--from-cache` で
解析だけやり直せるので、**貴重な取得予算を1回も使わずに直せた**
（設備の `re-extract`・経路の `re-segment` と同じ考え方）。
保存していなければ ATHOME の残り1リクエストを解析の試行錯誤で使い切っていた。

⚠ **市区の同定は索引に埋まっている JIS5桁で行う。** HOMES は
`id="city-13101-label-part-1"`、ATHOME は `name="areaList[11245]"` に入っている。
部分文字列一致は使わない（Phase 2 で名古屋市に北名古屋市の `23234` が混入した
原因 → ADR 0014）。JIS とリンク文字列の両方が引けて食い違う行は捨てる仕組みも
入れたが、**実測では食い違い0件**だった。

⚠ **収集そのものもリクエスト数を絞った**（HOMES 3・ATHOME 3。ATHOME は15秒間隔）。
課題#21（3秒間隔で47都道府県を回して検知が発動）を繰り返さないため。

#### 実装内容

| 区分 | 内容 |
|---|---|
| スキーマ | `t_site_scan_cursors`（`(site_id, pattern_name)` PK・`last_city_jis`・`last_scanned_at`・`last_run_id`）。マイグレーション `a4f7d3e91c26`。**既存テーブルは無変更** |
| 切り出し | `scrape/rotation.py`（`rotate_areas` / `next_cursor`）。**DBに触らない純関数** |
| 排他 | `persist.claim_city_rotation` / `advance_city_rotation` |
| 宣言 | アダプタのクラス属性 `city_rotation_limit`（HOMES 5・ATHOME 4） |
| マスタ | `db/seed/09_city_site_values_phase5e.sql`（新規318行）。HOMES を 2.5秒へ戻し ATHOME を `is_active=true` へ |
| テスト | 591 → **646件**。`tests/test_rotation.py` を新設 |

**設計で外せなかった4点**（いずれも外すと黙って壊れる）:

1. **カーソルは位置番号でなく JIS5桁で持つ。** 市区リストは YAML 編集で増減する
   （課題#32 で実際に4市区を外した）ため、番号だと編集のたびに別の市区へ飛ぶ。
   「カーソルより大きい最初の JIS から n 件、末尾に達したら先頭へ戻る」で周回する
2. **1回の実行では1帯だけが枠を使う。** HOMES は両帯の `sites:` に載っており、
   素朴に実装すると 5+5=10 リクエストが飛んで後半の帯が全部 202 になる。
   `last_scanned_at` の古い帯（未実行が最優先）が取り、`last_run_id` で
   同一実行の二重消費を防ぐ
3. **カーソルは取得を試みる前に進める。** 失敗した市区を再試行し続けると、
   そこから先へ永久に進めない。**実測でこれが効いた**（ATHOME は3・4件目が
   認証ページだったがカーソルは進んだ）
4. **`--full` でも1ページ・詳細0件に固定する。** 上限は「リクエスト数」に
   掛かっているので、2ページ目や詳細を1件でも取ると一覧の市区がそのぶん取れなくなる

#### 実測（2026-09-03・`scan --seed --site {HOMES,ATHOME}`）

| 確認したこと | 実測 |
|---|---|
| 帯1が枠を取り、帯2は譲る | 帯1 取得95件・新規18件 / 帯2「他パターンが取得枠を使用中」 |
| **次の実行で帯が交代する** | 2回目は帯1が譲り、帯2のカーソルが `11105` へ進んだ |
| カーソルが JIS で進む | HOMES 帯1 `13105`（文京区まで）/ ATHOME 帯1 `13104` |
| **失敗しても次へ進む** | ATHOME は3・4件目が認証ページだったが**カーソルは進んだ** |
| 掲載が貯まる | HOMES **10件 → 28件** |
| 0件のバックストップが働く | 「一覧が0件。過去に 28件 取り込んでいるサイトなので異常の疑い」 |

#### Phase 5E で判明した想定外のこと

| 事象 | 対応 |
|---|---|
| **予算は時間で回復する（プロセス単位ではない）** | 2分後に2回目を流したら HOMES の5リクエストが**全て 202** になった。取得タスクは2時間ごとなので実運用では収まる見込みだが、**回復窓の長さは未測定** |
| **ATHOME が23区帯で MUST 1段目を1件も通らない**（取得131件 → 通過0件） | `SORT=7`（賃料が安い順）で1ページ目だけを見るため、千代田区・中央区では30㎡未満の 1K/1R ばかりが返る。⚠ **ローテーションの不具合ではない**（面積下限をサイト側へ渡せていないのが原因で、`supports_site_filters` が未宣言）。直すには ATHOME の面積・間取りキーを実サイトで測る必要があり（推測で書かない → 課題#29）、予算が回復してから行う |
| **千代田区がサイト側フィルタで0件になり「疑い」が発火する** | 千代田区で 30㎡以上・10万円以下は実在しないので**正しい絞り込み**。対照取得は「フィルタが0件の原因か」しか判別できないので、これは仕様どおりの偽陽性。⚠ ただし1周（約1.4日）に1回エラー通知が飛ぶ |
| ATHOME の市区リンクが単一引用符だった | 上記「罠1」。取得HTMLの保存で救われた |

#### 残作業

1. **ATHOME の東京都府中市スラグ**（東京都索引を1リクエスト）
2. **予算の回復窓の実測**。測れれば「N回に1回の詳細回」を織り込める
3. ~~**ATHOME のサイト側フィルタ**（面積下限）~~ → **完了（2026-09-03・課題#39）**。
   `MENSEKI`（面積下限）・`EKITOHO`（駅徒歩）を配線した。⚠ **キー名と選択肢は
   一覧フィクスチャの検索フォームから採れた**ので、取得予算を1本も使わずに確定し、
   実サイトは検証（4リクエスト）だけに使った。→ §16.16
4. **管理者権限のある PowerShell** から `scripts/register_tasks.ps1`（→ 課題#23）
5. `scripts/register_tasks.ps1 -EnableScraping` で取得タスクを有効化

### 16.15 NIFTY の取り直しと詳細キューの掃き出し（2026-09-03 実施）

Phase 5E とあわせて実施した運用作業の記録。

#### NIFTY の取り直しは失敗した（405 の正体が判明 → 課題#38）

`run_initial_scan.ps1 -Site NIFTY` を走らせたところ、**両帯とも `aborted`**（失敗5件）。
調べると **HTTP 405 の本文が `<title>Human Verification</title>` の検知ページ**だった。
メソッド不許可ではなく、HOME'S の AWS WAF・ATHOME のパズル認証・MINIMINI の
reCAPTCHA と同じ**能動的なボット検知**である。

⚠ **同じ日のうちに解消し、また出た。** 午前は10市区を3秒間隔で全件成功したのに、
夕方は全市区で 405 になった。**単発の疎通確認で「解消した」と判断してはいけない。**
⚠ **ブラウザ相当UAでも同じ**なので、HOME'S のような「UAの選別」ではない。突破しない。

⚠ **打ち切りは正しく働いた**（課題#25 の修正が効いている）。271回叩き続けるような
ことは起きず、帯ごとに5件で止まった。

**副産物としてログの診断性を直した**（PR #33）。`SiteFetcher._fail` の例外は
「取得に失敗しました: {url}」だけで、`raise ... from` の連鎖は `t_scrape_logs` へ
`str(exc)` で書かれる時点で落ちていた。そのため **405 とタイムアウトと 403 を
ログから区別できず**、原因を調べるのに実サイトを叩き直す必要があった。
理由を本文へ入れるようにし、実際に `NIFTY: 5 回連続で失敗したため打ち切ります（HTTP 405）`
と出るようになったことを本番ログで確認した。

#### 詳細キューの掃き出し（`-Drain`・2時間42分）

15:05 → 17:47。`scan --seed --detail-limit 800`（一覧1ページ）を全サイト×2帯で回した。

| 指標 | 前 | 後 |
|---|---:|---:|
| 掲載 | 10,322 | **11,843** |
| 詳細未取得 | 1,337 | **97**（うち HOMES 73・ATHOME 8 はローテーションのため設計どおり） |
| 名寄せグループ | — | 7,399（グループ化された掲載 11,775） |
| 帯1 採点 / MUST通過 | 868 / 292 | **1,324 / 747** |
| 帯2 採点 / MUST通過 | 2,197 / 1,820 | **3,244 / 2,749** |

ランキングは健全なまま（帯1は葛飾・足立・江戸川など**23区のみ**、
帯2は鎌ケ谷・さいたま市・松戸・流山など**対象市区のみ**）。

#### 市区ローテーションが実運用で効いていることを確認した

| サイト | 掲載 | ローテーションの実績 |
|---|---:|---|
| HOMES | 10 → **77** | 帯1 カーソル `13105` → `13110`（目黒区まで）/ 帯2 `11105` |
| ATHOME | 0 → **8** | 帯1 `13104` / 帯2 `11104` |

⚠ **予算の回復が実測できた。** 1回目の HOMES は取得95件・新規18件だったが、
**約30分後の2回目は取得196件・新規49件**で、明らかに多く取れている。
（2分後の再実行では全て 202 だった → 回復窓は2分より長く30分より短い）

⚠ **ATHOME は近郊60分圏帯なら取れる**（237件取得・8件が MUST 通過。
さいたま市西区5・見沼区3）。23区帯で0件だったのは相場の問題で、
**帯によって結果が違う**ことが実測で確定した（→ 課題#39）。

### 16.16 ATHOME のサイト側MUST（2026-09-03・課題#39）

Phase 5E で市区ローテーションに載せた ATHOME が、**23区帯で一覧131件を取得して
MUST 1段目の通過が0件**だった件の対応。→ 課題#39・[ADR 0015](./adr/0015-site-side-must-filters.md)

#### 実サイトを叩く前に、フィクスチャから候補を確定できた

⚠ **キー名と選択肢は一覧フィクスチャ（`tests/fixtures/athome/list_page1.html`）の
検索フォームにそのまま埋まっていた。** `<select name="MENSEKI">` の option を読むだけで
「30㎡以上＝`kt004`」まで分かる。**取得予算（1回4リクエスト）を1本も使わずに
候補を確定でき、実サイトは検証だけに使えた。**

⚠ この手は **HOMES・GOO・APAMAN でも使えた**（いずれも一覧に検索フォームがある）。
取得数に上限があるサイトでは、これを最初にやるかどうかで進み方が変わる。

#### 実測（足立区 `tokyo/adachi-city`・`SORT=7`）

⚠ **ATHOME は総件数を出さない**ので、GOO・APAMAN と同じく**返る掲載の中身**で
確かめた。判定方法の妥当性は `zzz=1` で先に担保している。

| 送った値 | 返った掲載 |
|---|---|
| 基準 `SORT=7` | 109件・面積 18.0〜64.5㎡（**30㎡未満33件**）・徒歩 2〜28分（**20分超47件**）・間取り `{2LDK:31, 1LDK:23, 3LDK:23, 1K:19, ワンルーム:13}` |
| `zzz=1`（対照） | 109件・**分布が基準と同一**（存在しないキーは黙って無視される） |
| `MENSEKI=kt004` | 63件・面積 **36.1〜101.1㎡（30㎡未満0件）** |
| `EKITOHO=ke006` | 66件・徒歩 **5〜19分（20分超0件）** |
| `MADORI[]` ×5種 | 96件・間取り **`{1LDK:34, 2LDK:30, 3LDK:29, 2DK:3}`＝MUSTの5種だけ**・賃料の下限が 69,000 → 98,000円 |

#### 効果は「枠の節約」より強い

⚠ **`SORT=7`（賃料が安い順）に面積下限が掛かると、1ページ目の意味が変わる。**
「最も安い30件」から「**面積・間取り条件を満たすうち最も安い30件**」になる。
MUST を通る掲載は必ず賃料上限以下なので、**通る掲載があれば1ページ目に必ず載る**。
つまり「構造的に見つからない」から「あれば必ず見つかる」への変化であって、
単に無駄が27%減るという話ではない。

#### 設計で外せなかった点

| 点 | 理由 |
|---|---|
| **選択肢はコードで、値から算術で導けない** | 徒歩は 1→`ke102` / 3→`ke002` / 7→`ke101` と**連番ですらない**。`enum` に `codes` の対応表を足した |
| **`choices` と `codes` は必ず揃える** | 片方だけ増やすと丸めは通るのにコードが無い状態になる。`ParamError` で落とし、正典YAML全体を走査する回帰テストも置いた |
| ⚠ **JSONB はキーを文字列にする** | 正典YAMLでは `20: kt002` と数値キーだが、`m_site_search_params.value_spec` から読むと文字列キーになる。**実行時はDBから読むので、片方しか引けないと本番だけ壊れる** |
| **ローテーションサイトでは対照取得をしない** | 上限は**リクエスト数**に掛かるので対照の1本が市区1つぶんの枠を食う。しかも対照自身が上限を踏んで検知ページを掴み、「対照取得に失敗」という**原因を取り違えたエラー**が出るだけ |

#### S付き間取りは ADR 0015 に対する既知の例外

⚠ **`km005`（1LDK）が `1SLDK` を含むかは測れていない。** コード体系に欠番があり
（`km005` の次が `km008`＝2K で、部屋数ごとに5枠ある形）、S付きが別コードである
可能性が高い。ローカルの `normalize_layout` は S を落として比較するため
`1SLDK` は MUST を**通る**ので、別コードなら**サイト側フィルタが MUST より厳しくなる**。

ユーザー判断で**承知のうえ配線した**（実データのS付きは 44/11,843件＝**0.37%**、
かつ他サイトにも載っていれば名寄せで拾える。対して対象外の間取りで枠が埋まる損失は
実測で63件中17件＝**27%**）。⚠ **この例外を他サイトへ広げない。**

⚠ **あわせて ADR 0015 の事実誤りを訂正した。** 初版は SUUMO について
「返る `1SLDK` はローカルの MUST で落ちる（余分に取っているだけ）」と書いていたが、
S を落として比較するので**通る**。SUUMO が上位集合を返すのは無害なのではなく、
**不変条件を満たすために必要**だった。

#### 予算の回復は「時間で1本ずつ戻る」ではなかった

課題#36 は回復窓を「2分より長く30分より短い」としていたが、今回の実測で
**モデルそのものが違う**と分かった。

| 時刻 | 出来事 |
|---|---|
| 18:15〜18:16 | 4本すべて成功（上限ぶん） |
| 18:26 | 1本成功（10分後・部分的に回復） |
| 18:28・18:29 | **2本とも認証ページ** |
| 19:05〜19:06 | **4本とも認証ページ**（前の失敗から36分空けても戻っていない） |

⚠ **失敗した試行そのものがブロックを延長している疑いが濃い。**
予算切れのときに探りを入れるのは、待ち時間を伸ばすだけで損になる。

#### 実地検証（2026-09-03 20:00・近郊60分圏帯）

⚠ **1回目の検証スキャンは4本とも認証ページで空振りした**（予算切れ）。
90分空けて再試行したところ通り、**効果が本番で確認できた**。

| 指標 | 修正前 | 修正後 |
|---|---:|---:|
| 取得 | 237件 | 167件 |
| **MUST 1段目通過** | **8件** | **31件** |
| 通過率 | 3.4% | **18.6%** |
| ATHOME の掲載（累計） | 8件 | **39件** |
| うち30㎡未満 | — | **0件** |

⚠ **取得件数はむしろ減っている**（237 → 167件）のに通過が4倍近い。
サイト側で母集団を削ったぶん、同じ1ページ枠に**使える掲載だけ**が載るようになった。

⚠ **23区帯の実地確認はまだ**（1回目の空振りでカーソルだけ進み `13108` に、
`last_scanned_at` が新しくなったため、再試行では近郊帯が枠を取った）。
次のローテーションで23区帯に枠が回るので、取得タスクを有効化すれば自然に検証される。

あわせて本番ログで次を確認した。
①URLが意図どおり組める（`?SORT=7&PRICETO=kc121&MENSEKI=kt004&MADORI[]=…&EKITOHO=ke006`）
②**ローテーションサイトで対照取得が走らない**（従来なら出る「対照取得に失敗」が無い）
③0件のバックストップが発火する ④**失敗してもカーソルが進む**（`13104` → `13108`）


### 16.17 Phase 5F UR賃貸住宅（2026-09-03 完了）

**完了条件「URの住戸がランキングまで貫通する」を達成**（701テスト緑）。
→ [ADR 0019](./adr/0019-ur-api-post-and-robots-403.md)・課題#37・詳細設計書 §9.3

#### 着手前の実測で設計案の記述が3つ覆った

前セッションでまとめた設計案（§16.13）には、実測で誤りと分かった記述が3つあった。
**どれもエラーにならない類**なので、測らずに書いていたら静かに壊れていた。

| 設計案の記述 | 実測 |
|---|---|
| 検索APIは「`pageIndex=0`（1ページ20件）」 | **ページ送りは無い**。`pageIndex` は黙って無視され、そのエリアの**全団地が1応答で返る**（東京 area=06 は117件） |
| 住戸APIの応答に `year`/`kouzou`/`traffic`/`place`/`feature` がある | **すべて `None`**。設備原文・築年は**3つ目のAPI `detail_room`** にある（設計案はこのAPIの存在を知らなかった） |
| `vacancy=1` で空室のある団地に絞れる | **黙って無視される**。空室情報は既定で入っている |

⚠ **判定方法そのものを危うく誤診するところだった。** 最初 `area=01` で測ったところ
7通りすべてが**同一バイト数**で返り、対照の `zzz=1` も「同一」だったので
**判定方法として妥当**と読めてしまう。実際は `area=01` が20団地・空室0の
小さなエリアで、**どのパラメータも効いていなかった**だけである。

→ **対照には「効かないはずのキー」だけでなく「効くはずのキー」も置く。**
`tdfk=11` を送って埼玉の団地が返ることを同時に確かめ、
「差が出る経路は生きている」ことを担保してから各パラメータを測った。

⚠ この見落としを避けられたのは、**取得した応答をすべて保存していた**からでもある
（`scripts/tools/probe_ur.py` の `--cache-dir` / `--from-cache`）。
ATHOME の市区リンクの件（→ 課題#36）と同じ備えが効いた。

#### 実装（既存10アダプタには一切触っていない）

| 区分 | 内容 |
|---|---|
| 取得 | 3段のJSON API（**すべて POST**）。①団地一覧 ②住戸一覧（5件/ページ）③住戸詳細 |
| 対応づけ | **①＋②が「一覧」・③が「詳細」**。③が `detail_fetched_at IS NULL` のキューに乗るので `--detail-limit` がそのまま効く |
| 拡張方式 | 任意フック `collect_listings` / `fetch_detail` を宣言したアダプタにだけ `pipeline.scan` が委譲する（`supports_site_filters`・`city_rotation_limit` と同じ宣言ベース）。`SiteScraper` Protocol は変えない |
| 共通化 | `SiteFetcher.get` の本体を `request(method, url, data=)` へ一般化し `get`/`post` を薄いラッパに。**レート制御・robots判定・バックオフ・打ち切り・日次上限はそのまま共有** |
| robots | APIホストの robots.txt が **HTTP 403**（不在ではない）。`ignore_robots` は立てず、**オリジンごとに1回だけ警告を記録**する |
| エリア | 市区で検索する手段が無いので、都県の全 area を列挙して応答の `skcs`（市区名）で**ローカルに絞る** |
| 初期費用 | 礼金・仲介手数料・更新料・保証人不要を**合成トークン**で設備原文へ入れ、既存条件へ**辞書無変更**で載せる（EHEYA の `_FLAG_TOKENS` と同じ手法） |
| テスト | 647 → **701件**。`tests/fixtures/ur/` は**実APIの応答をそのまま保存**したもの（UR は空室が早く埋まるので、取れたときに固定しないと同じ状態を作り直せない） |

**設計で外せなかった点**:

- ⚠ **フックに `(listing, detail)` の対を返させない。** 詳細まで一覧の段で取ると
  **`--detail-limit` が効かなくなる**（帯2は住戸が数百あるので、1回の実行で
  数百リクエストを一覧フェーズに押し込むことになる）。③を詳細キューに乗せれば
  既存の分割がそのまま効く
- ⚠ **①と②でページ送りの有無が逆。** ①で `pageIndex` を回すと同じ団地を何度も取り、
  ②で回さないと5件目以降を永久に取り逃す
- ⚠ **②は末尾のページを超えると `null` を返す**（空配列ではない）。
  そのまま反復すると `TypeError` になる
- ⚠ **②と③は同じキー名でも中身が違う。** ③では `type`/`floorspace`/`commonfee` が
  `None` で、代わりに `madori`/`madoriYuka`/`commonfee_sp` に入る
- ⚠ **`area` 区分の静的な対応表は作らない。** UR が区分を変えても気づけないまま
  市区が落ちる（`resolve_areas` が検索値の無い市区を黙って落とすのと同じ失敗 → 課題#36）。
  01 から順に叩き、空応答が2回続いたら止める

#### 実測値

**`area` 区分**（1都3県で **702団地**・うち空室のある団地155）

| 都県 | `tdfk` | area | 団地 | 空室あり | 区分の中身 |
|---|---:|---|---:|---:|---|
| 東京都 | 13 | 01〜06 | 308 | 47 | 01〜05＝23区（空室9） / **06＝多摩**（117団地・空室38） |
| 埼玉県 | 11 | 01〜04 | 118 | 48 | 01 東部 / 02 さいたま市 / 03 中央 / 04 西部 |
| 千葉県 | 12 | 01〜07 | 110 | 24 | 01 千葉市内陸 … 07 成田 |
| 神奈川県 | 14 | 01〜06 | 166 | 36 | 01 川崎市 / 02〜04 横浜市 / 05 県央 / 06 湘南三浦 |

⚠ **`skcs` は政令市で行政区まで入り**（`さいたま市中央区`・`横浜市港北区`）、
`m_cities.canonical_name` と同表記なので検索パターンの `cities` とそのまま突き合わせられる。

**サイト側MUST**（→ ADR 0015）。効いたのは `rent_high` だけだった。

| 送った値 | 結果 |
|---|---|
| `rent_high=99999` | 団地117のまま・**空室のある団地 38 → 18**・空室 101 → 69室 |
| `rent_high=100000` | 99999 と**同一**（端数で0件にならない） |
| `floorspace_low=30` | **基準と完全同一**（黙って無視される） |

⚠ **`rent_high` は団地を絞らず `roomCount` を絞る。** ②③を叩く団地が半減するので
**リクエスト数に直接効く**（1エリア約132 → 約86）。

**実地の確認**（`scan --seed --site UR`）

| 帯 | 取得 | MUST1段目通過 |
|---|---:|---:|
| 東京23区賃貸 | 6 | **0** |
| 近郊60分圏賃貸 | 33 | **0** |

⚠ **完了条件「URの住戸がランキングまで貫通する」は、配線としては達成したが
実データでは1件も通っていない。** 経路そのものは
「①②で一覧 → MUST → ③で詳細 → 設備抽出36件 → スコア」まで実際に流れた
（下記の不具合1で `price` が NULL のまま MUST を `unknown` ですり抜けた3件が
偶然その経路を通り、配線が生きていることを本番DBで示した）。
不具合を直すとその3件は `rent_total` 107,600円で正しく落ちる。

**通らない理由は実装ではなく条件**（33件すべてを1件ずつ判定して確認した）。

| 落ちた理由 | 件数 | 例 |
|---|---:|---|
| `rent_total_max`（帯2は70,000円） | 31 | 睦町 73,800円 / うらわイーストシティ 96,300円 |
| `layouts`（**3DK・1DK・1K・4LDK が対象外**） | 13 | 習志野台 **67,050円・50㎡・徒歩9分・3DK** |
| `walk_minutes_max` | 1 | 大津ヶ丘 58,000円・59㎡・**徒歩33分**・3DK |

⚠ **UR は1都3県の対象市区では 7〜11万円台が中心**で、帯2の 70,000円に収まる住戸が
ほとんど無い（空室のある61団地のうち賃料下限が7万円以下なのは**3団地だけ**）。
⚠ **`layouts` に 3DK が入っていない**のが効いている。2DK と 3LDK は対象なのに
その間の 3DK だけが外れており、UR では最も多い間取り（33件中8件）。
習志野台の1件は**賃料・面積・徒歩のすべてを満たし、間取りだけで落ちている**。
→ **MUST を緩めるかはユーザー判断**（⚠ 緩める方向は取り直しになる。
MUST 1段目で `fail` した掲載はDBに残らないため → ADR 0013）。

⚠ **23区は191団地に対し空室が9団地しかなく、その最安が 100,800円**（MUST は100,000円）。
UR は空室が出ると早く埋まるので、**新着通知の価値が高い一方で1回に拾える住戸は少ない**。


#### 実地の確認で「黙って壊れる」不具合を2件見つけた

⚠ **どちらもテストが緑のまま本番DBにだけ現れた。** フィクスチャで固定してある。

1. **割引適用の住戸は `rent` が空文字で、賃料が `rent_normal` に入る**
   （サイト表示も「割引適用 前家賃 103,600円／家賃についてはお問い合わせください」）。
   見落とすと `price` が NULL → `rent_total` が NULL → **MUST が `unknown` へ落ち、
   `unknown_policy: keep` の下で賃料不明の掲載がランキングに並ぶ**。
   例外にならないので気づけない（さいたま市南区の3件がこれだった）。
   ⚠ 敷金も賃料から算出するので**連鎖して NULL になっていた**
2. **1行に「徒歩◯分 または バス◯分 徒歩◯分」と選択肢が並ぶ**
   （`JR武蔵野線「東浦和」駅 徒歩16～19分 または バス3分 徒歩2～5分`）。
   ⚠ バス経由の徒歩は**バス停からの徒歩**なので駅徒歩にすると
   `walk_minutes_max` を不当に通過するが、**行ごと弾くと本物の駅徒歩16分を捨てる**。
   「または」で選択肢に割ってから1つずつ見る

#### あわせて処理したユーザー判断

| 論点 | 決定 |
|---|---|
| UR の初期費用の有利さをランキングへ載せるか | **両帯の WANT へ `PRICE_NO_REIKIN` 5・`PRICE_NO_GUARANTOR` 3 を追加**（設備の weight 合計 110 → 118）。⚠ **UR 専用ではなく全サイトに効く**ので、他サイトの礼金なし物件も同じだけ加点される。`config_hash` が変わるので次回の採点で自動的に再スコアされる |

#### 残作業

1. ~~**MUST を緩めるかのユーザー判断**~~ → **決定（2026-09-03）: 両帯の `layouts` に
   3DK を追加した**（`rent_total_max` は据え置き）。UR で最多の間取りで、
   習志野台の1件は賃料・面積・徒歩をすべて満たし間取りだけで落ちていた。
   ⚠ **UR 専用ではなく全サイトに効く**（既存DBは 2DK 5,157件に対し 3DK は0件）。
   ⚠ **サイト側MUSTは無傷**（SUUMO `md=09` / HOMES `cond[madori][33]` /
   GOO `lo0303` / ATHOME `km014` がすべて存在する。1つでも欠けると
   `layouts` 軸ごと送らなくなり、ATHOME で課題#39 が再発する）。
   ⚠ **取り直しは `scan --seed`**（通常の scan だと数百件が一斉に「新着」通知になる）
2. **連続取得時のスロットリング・検知の観測。** ⚠ HOMES・ATHOME・NIFTY は
   いずれも**1リクエスト目が正常に返る**ので単発では再現しない。
   取得タスクを有効化すれば定期スキャンで自然に観測される
3. **住所が市区までしか取れない**ので UR の掲載は `dedup_key` が作れず単独で残る
   （他サイトと重ならないので名寄せの実害は無い）
4. **管理者権限のある PowerShell** から `scripts/register_tasks.ps1`（→ 課題#23）
5. Phase 5G/5H の5サイト（レオパレス21・D-room・ハウスコム・ホームメイト・CHINTAI.net）


### 16.18 通勤時間の欠損を潰す（2026-09-04・課題#40・#41）

UR の掲載「習志野台 7-3号棟406号室」が `must_result=unknown` のまま
1,675位にとどまっていた件を調べたところ、**通勤時間の配線に3つの欠陥**が見つかった。
いずれも**例外にならず、ランキングを見ても気づけない**類である。

#### 欠損していたのは「通勤時間」と「築年」の2つ（駅徒歩は正常）

`score_breakdown` を読むと `commute_minutes`（weight 25）と `age_years`（weight 15）が
`missing: true` で、`walk_minutes` は9分で正常だった。

| 欠陥 | 原因 | 影響範囲 |
|---|---|---|
| **駅が1件も同定できない** | matcher が鉤括弧を区切り文字として扱い、`「北浦和」駅` の「駅」の直前が `」` になって第1パスが空振り（→ 課題#41） | **ATHOME 52掲載の全件** ＋ UR |
| **UR が「駅」を落としていた** | `parse_access` が `｢高尾｣駅` から `高尾` だけを採る。アンカーが無いと第2パスにも掛からない | UR 全件 |
| **UR の築年が未配線** | API は築年月ではなく**築年数**（`year`=59）を返す。`type_specific_attrs.ur_age_years` に置くだけで `t_listings.age_years` へ渡していなかった | UR 全件 |

⚠ **`t_listing_stations` に行すら残らない**のが厄介だった。同定に失敗した表記は
`unmatched` として記録される仕様だが、**第1パスが0件・第2パスがマスタ不一致**という
組み合わせでは1行も入らないため、「同定できなかった表記」の一覧にも出てこない。

⚠ **Phase 5C の同定率99.4%は ATHOME の掲載が0件のときの値**だった（ボット検知中 →
課題#20）。Phase 5E で復帰して52件入ったが、**同定率を測り直していなかった**。

#### 調査の途中で、より重大な欠陥が見つかった（課題#40）

目的地別に `t_station_commutes` を数えたところ、**芝公園ゆき1,155駅の実ダイヤが
全件 `rail_graph`（回帰式）に戻っていた**。最終更新は当日 00:31 の `scan`。

⚠ **`scan` の `_refresh_commute` にだけ保護が抜けていた。** ADR 0017 決定6・
CLAUDE.md・要件定義書はいずれも「回帰式で実ダイヤの行を上書きしない」と書いており、
`resolve-commutes`（CLI）は `origins_with_source` で守っていたが、
**同じ回帰式を書き戻す経路がもう1つあった**。
⚠ 例外にならず行数も減らず、変わるのは `source` 列だけなので**気づけない**。
⚠ 取得タスクを有効化していれば**2時間ごとに潰れ続けていた**。

**再取得（4.8時間）は要らなかった。** `t_navitime_routes` に候補ごとの `total_minutes` が
残っているので、`re-segment` に `restore_commutes` を足して**1回で1,155駅を戻した**。
ADR 0017 決定3（経路の原文を残す）が効いた3つ目の場面である。

#### 実測（2026-09-04）

| 指標 | 修正前 | 修正後 |
|---|---:|---:|
| ATHOME の駅同定 | **0 / 52（0.0%）** | **52 / 52（100%）** |
| UR の駅同定 | 0 / 1 | 1 / 1 |
| 芝公園ゆきの実ダイヤ | **0駅** | **1,155駅** |
| 掲載19964 の `must_result` | unknown | **pass** |
| 掲載19964 の順位 | 1,675位 | 1,628位 |

⚠ **順位はほとんど変わらない。** 欠損を埋めると `age_years`（築59年）が
weight 15 の分母に乗って0点になるため、スコアはむしろ 33.7 → 30.0 へ下がる。

**1,628位の理由は欠損ではなく実力**だった（採点対象2,048件・中央値42.3点に対し30.0点）。

| 項目 | 得点 | 上位5件 |
|---|---|---|
| `rent_total` 67,050円 | **5.9 / 40** | 48,000〜60,900円 |
| `age_years` 築59年 | **0 / 15** | 築21〜39年 |
| `commute_minutes` 56分 | **5.0 / 25** | — |
| 設備 | **6 / 24 項目** | — |
| `area_sqm` 50.0㎡ | 20 / 20（満点） | 37.9〜47.9㎡ |
| `walk_minutes` 9分 | 6.5 / 10 | 3〜16分 |

⚠ **「MUST を通った」で解決としない**という判定基準が効いた。MUST だけを見れば
`unknown → pass` で直ったように見えるが、順位で見ると**この住戸は面積以外の全項目で
上位に負けており、UR の初期費用の有利さ（礼金・保証人なしで weight 8）でも埋まらない**。
MUST を緩める（`rent_total_max` を上げる）以外に上位へ来る道はない。


### 16.19 定期実行の開始（2026-09-04・課題#23・#8）

**タスクスケジューラ4本＋週次1本の登録と有効化が完了した。** Phase 5 の残作業として
§16.7 以降のすべての節に「管理者権限のある PowerShell から `register_tasks.ps1`」が
残っていたが、実測で**5本すべてが登録済み・有効**であることを確認した。

| タスク | トリガー | 状態 | 実行ユーザー | 前回実行 |
|---|---|---|---|---|
| `HouseSearch-Scan` | 2時間ごと・01:15起点 | Ready（有効） | `wy469` | 2026-09-04 01:15（結果 0） |
| `HouseSearch-Sweep` | 毎週日曜 02:00 | Ready（有効） | `wy469` | 未実行 |
| `HouseSearch-CheckSold` | 毎日 09:00 | Ready（有効） | `wy469` | 未実行 |
| `HouseSearch-Digest` | 毎日 20:00 | Ready（有効） | `wy469` | 未実行 |
| `HouseSearch-Backup` | 毎日 03:30 | Ready（有効） | `wy469` | 未実行 |

取得タスクも有効なので `-EnableScraping` まで済んでいる。実行ユーザーが `wy469` なので、
課題#23 が警告していた自己昇格の罠（UAC で管理者の資格情報を入力すると
**昇格後のプロセスがその管理者として動き、タスクがそのアカウントで登録される**）も
踏んでいない。

#### 01:15 の Scan がスキップされたのは設計どおり

「他の取得処理が実行中のためスキップしました」で終了しているが、これは当時
手動スキャンが `pg_advisory_lock` を持っていたためで正常な動作である（→ ADR 0013 決定8）。

⚠ **`fetch-commutes` はこのロックを取らない。** そのため全国取得（→ ADR 0018・
約36.5時間）の最中でも2時間ごとの定期スキャンは走る。⚠ **これは意図した設計**で、
NAVITIME と物件サイトは別ホストなのでレート制御が干渉しないため。
⚠ 逆に**同じ NAVITIME を叩く `fetch-commutes` どうしを並走させてはいけない**。
実効間隔が 15秒 → 7.5秒になり、robots.txt の `ClaudeBot` 向け `Crawl-delay: 10` を破る。
地方の取り直しは進行中の取得が終わってから行う。

#### 「5本すべて無効で登録されている」は再現できなかった

2026-09-03 の引継ぎメモは、Digest（20:00）と Backup（03:30）まで無効で登録されていると
記録していた。⚠ **`register_tasks.ps1 -DryRun` が生成する XML を読み直したところ、
`<Settings><Enabled>` は取得3本 `false` / Digest・Backup `true` で仕様どおり**だった
（2026-09-04 実測）。スクリプト側の不具合ではない。

既に再登録されているため当時の観測は再現できず、原因は不明のまま残る。
⚠ **登録操作には「意図どおり登録できたか」を自動で確かめる手段がない**ので、
再登録したら毎回 `Get-ScheduledTask -TaskName 'HouseSearch-*'` で
`State` と `Principal.UserId` を目視すること。

#### 残っている確認

1. **Backup のタスク経由の初回実行**（次回 2026-09-04 03:30 → 課題#8）。手動実行では
   検証済みだが、⚠ **タスクとして走ったときに `PGPASSWORD` を渡せるか・出力先へ
   書けるかは別の話**なので、`F:\backups\searching-for-houses\` の世代と
   タスクの「前回の結果」を見る
2. **23区帯での課題#39 の実地確認**。2時間ごとの定期スキャンが回るので自然に済む
   （近郊帯では確認済み。23区帯のカーソルは `13108`）


### 16.20 Phase 5H ハウスコム・ホームメイト（2026-09-04 完了）

**課題#37 のチェックリスト10項目を実装前に全部測り、2サイトを実装して本採用した**
（772テスト緑 → 詳細設計書 §13）。CHINTAI.net は見送り（→ 課題#43）。

#### 実測サマリ

| 項目 | ハウスコム | ホームメイト |
|---|---|---|
| ホスト | `www.housecom.jp` | `www.homemate.co.jp` |
| 市区の検索値 | サイト固有スラグ（4本で224市区・帯82/82） | **JIS5桁そのもの** |
| ページ送り | `?page=N`（1始まり） | `?pg=N`（1始まり） |
| サイト側MUST | 間取り `cc_mdr[]`（クエリ配列）＋`sort=0` | `hs` 面積下限＋`ye` 賃料上限＋`so=11` |
| 連続取得 | **20市区正常** | **20市区正常** |
| MUST 1段目通過 | 23区 **82** / 近郊 **331** | 23区 **102** / 近郊 **165** |
| **ユニーク率** | **81%（全13サイト中で最高）** | 49% |

#### ⚠⚠ この Phase で最も危なかった2つの誤り（どちらも実測で覆った）

**① 1ページ目だけで分布を判断して、危うく不採用にするところだった**

ハウスコムの足立区一覧（既定の並び）は1ページ目127住戸が
**rent_total 中央 155,000円・10万円以下が0%**。§12.5 で D-room に効いた
「分布を先に測る」手をそのまま当てると「賃料が高すぎて使えない」という結論になる。
実際は `cc_mdr-4/?sort=0`（1LDK・安い順）にすると**11住戸中9件が MUST を通過**した。

→ **「分布を先に測る」は並び順とセットでなければ誤診する**（課題#39 の裏返し）。

**② 単発の観測から掲載終了の判定を誤って一般化した**

`/room_1/` が `RenoasHAKATA ***号室` を HTTP 200 で返したので「募集を終えた住戸」と
解釈し、`is_sold` を号室の伏字で判定する実装にした。⚠ **実地の取り込みで覆った。**
一覧（安い順）の1ページ目にも伏字が並び、**本番413掲載のうち100件（24%）が伏字**で、
いずれも募集中だった。そのままなら**毎日の `check-sold` が募集中の掲載の4分の1を
ランキングから消していた**（エラーにはならない）。

→ **掲載終了の判定は 404 のみに直した。**
→ 教訓: **極端な値のID1本で解釈を決めない。「その状態が一覧にも現れるか」を確かめる。**

#### 重複の一次見積もりは「桁を外さない」用途に留める

実装前に「同市区で面積+賃料が一致するか」で重複を見積もり、**手法の偏りを
答えの分かっている D-room で検証した**（見積もり90%に対し真の答え65%＝過大評価）。

| サイト | 一次見積もりの重複 | 実測（`dedup-stats`） |
|---|---:|---:|
| ハウスコム | 22% | **19%**（近い） |
| ホームメイト | 12% | **51%**（4倍以上のずれ） |

⚠ **過大評価すると検証したのに、ホームメイトでは過小評価した。**
サンプルが足立区の MUST 通過8件しかなかったため。
→ **手法の偏りを1サイトで検証しても、サンプルが小さければその検証自体が当てにならない。**
採否の最終判断は実装後の `dedup-stats` で行う（賃貸EX・D-room と同じ手順）。

#### あわせて見つかったもの

- ⚠⚠ **`RobotFileParser` が `User-agent: *` の2つ目のグループを無視する**（→ 課題#43）。
  CHINTAI.net の robots.txt で発見。RFC 9309 §2.2.1 違反。既存15サイトは
  すべて1グループ以下で**実害は無い**ことを実測で確認したが、CHINTAI.net を
  実装するなら前提になる
- ⚠ **ホームメイトの robots.txt は `ClaudeBot` を名指しで全面禁止**している。
  名乗るUAは `house-search/2.0` なので `User-agent: *` が適用される。
  **`*` に従って取得するのはユーザー判断**（2026-09-04 → 詳細設計書 §13.2）
- ⚠ **既存テストが「12サイト」を期待したまま緑だった。** UR・レオパレス・D-room を
  足したときテストDBへ `db-seed --test-db` を流しておらず、期待値が古いまま
  検出されなかった。`db/seed/02_sites.sql` と突き合わせる形に変え、
  サイトを足すたびに黙って古くなることがないようにした

#### 実装で踏んだ「静かに壊れる」もの

| 事象 | 影響 |
|---|---|
| ハウスコムの市区スラグ収集で `_` を含めなかった | 政令市の行政区**27市区が黙って落ちる**（さいたま・川崎・横浜の全区＝帯2の主要エリア） |
| `cssselect` が**自分自身にもマッチする** | 入れ子の除外が効きすぎて**掲載0件**になった |
| `parse_area_sqm` に単位なしで渡した | 面積が全件 None（エラーなし） |
| 棟の階数が「地上4階」 | `parse_total_floors`（「4階建」を見る）が読めず全件 None |
| ホームメイトに**階の表記が無い住戸**がある | 階を必須にすると26住戸のうち8件が黙って落ちる |
| ホームメイトの物件番号に**全角数字** | URLエンコードされ `[0-9A-Za-z]+` が8件を落とす |
| ハウスコムが徒歩を**カッコで囲む**（`（徒歩12分）`） | matcher の第2パスが「徒」を拾う（第1パスは通るので実害は小） |

#### 残作業

1. **ホームメイトの駅同定が80%**（ハウスコムは98.8%）。交通欄をテキストから
   40文字幅で切り出しているため。DOM から取るようにすれば上がる見込み
2. **CHINTAI.net**（→ 課題#43 の robots 修正が前提）

### 16.21 Phase 5H CHINTAI.net（2026-09-04 完了・Phase 5H 完結）

**課題#43（robots 判定の欠陥）を先に直してから実装し、本採用した**
（863テスト緑 → 詳細設計書 §14）。これで課題#37 の6サイトがすべて片づいた。

#### 前提を先に直した — 課題#43

CHINTAI.net の robots.txt は `User-agent: *` を**2グループ**持つ。標準の
`urllib.robotparser.RobotFileParser` は2つ目以降を丸ごと落とすため
（`_add_entry` が最初の `*` だけを採る）、`/api/` や `/list/?b=` を
**許可と誤判定していた**。RFC 9309 §2.2.1 違反で、標準ライブラリ側の問題である。

⚠ **既存15サイトに実害は無かった**が、**相手の robots.txt はこちらの都合と無関係に
変わる**。「変わったことに気づく手段がない」（取得が成功してしまう）以上、
CHINTAI.net の実装可否と切り離して直した。

#### 前セッションの調査記録に誤りが2つあった

⚠ **§14.1 の「絞り込み」欄はキー名を挙げただけで、効くことを確かめていなかった。**
キャッシュに残っていた `*_adachi_base/filtered` を解析し直したところ、
どちらも**「家賃相場」ページ**（`cassette_item` が0件）だった。

| §14.1 の記述 | 実測 |
|---|---|
| 賃料上限 `ct=130000`（円） | ⚠ **選択肢の単位は千円**（13万円=`ct=130`）。円で送ると黙って無視される |
| 駅徒歩 `j=20`（分） | ⚠ **コード**（20分以内は `j=7`）。分で送ると黙って無視される |

⚠ **どちらも0件にならず基準と同じ件数が返る**ので、「絞れたつもりで絞れていない」
状態になる。SUUMO の `ct`（0件になる → 課題#29）より気づきにくい。

#### ⚠⚠ このサイトは「返る掲載の中身」で効きを判定できない

**一覧に返る住戸が実行のたびに入れ替わる**（同じURLで50件↔52件・面積の範囲も動く）。
GOO・APAMAN・ATHOME で使った手（→ §13.9）が通じず、
**総件数（本文の「N件」）だけが安定した判定材料**だった。

| 送った値 | 総件数 |
|---|---:|
| 基準 / **対照 `zzz=1`** | 11,109 / **11,109**（＝判定方法として妥当） |
| `sf=30` / `ct=130` / `j=7` / `m` 6種 | 5,969 / 6,662 / 10,771 / 5,832 |
| **4軸すべて** | **1,581** |

⚠ 並び順を指定しても揺れは止まらない。決定的な `o=2`（安い順）は
**棟あたり最安1住戸しか返さず母集団が半減する**（ハウスコムの `?sort=0` と同じ）ので、
**並び順は指定しない**。⚠ その結果**「新着を確実に拾う」ことはできない**。

#### 実装で踏んだ「静かに壊れる」もの4件

| 事象 | 影響 |
|---|---|
| ⚠⚠ **`detail_url` の実装漏れ** | **詳細取得が1,237回すべて失敗**。⚠ `Protocol` は実装を強制せず、フィクスチャテスト18件は緑のまま。⚠ **相手サイトへ無駄なリクエストを1,237回投げた** |
| ⚠⚠ `chinRyo` は `118000` の**生値**で `parse_yen` が読めない | **49件すべて `price` が None** → `rent_total` が NULL → MUST が `unknown` → 賃料不明の掲載がランキングに並ぶ（UR の割引住戸と同型） |
| ⚠⚠ `ct` の単位を**万円と取り違えた** | `ct=13` を送ることになり黙って無視される。**回帰テストが捕まえた** |
| ⚠ 住所セルに地図への導線が同居 | `dedup_key` が他サイトと一致せず**名寄せが黙って失敗する** |

`detail_url` の漏れを受けて **`tests/test_scraper_protocol.py` を新設**し、
全アダプタが Protocol の必須メソッド7つを持つことを機械的に突き合わせるようにした。
⚠ **属性は宣言を強制しない**（`scan` は `getattr(..., 既定値)` で読むので、
必須にすると既存9アダプタが落ちる）。

#### 実地の確認と採否

| 帯 | 採点 | MUST通過 | **最高順位** |
|---|---:|---:|---:|
| 東京23区賃貸 | 444 | 440 | 48位 |
| 近郊60分圏賃貸 | 1,065 | **818** | **13位** |

`dedup_key` 充足 **100%**・**駅の同定 100%**・設備 平均10.9件/掲載。

⚠⚠ **重複の一次見積もりが正反対に外れた。** 実装前は「ユニーク率65%以上」と
見積もったが、実測は **16%で全15サイト中の最低**だった（ホームメイト 12%→51% に続く2例目）。
⚠ 詳細を200件取っても評価は変わらなかった（代表 158 → 182 の微増のみ）。
→ **一次見積もりは採否の判断に使えない。**

**ユーザー判断（2026-09-04）: 本採用。** ①独自247件・代表182件は賃貸EX（339）に迫る
②取得コストが低い（上限なし・robots 全許可・両帯で約13分）
③**近郊帯で13位**と上位15件に実際に届いている。

⚠ **「ユニーク率が低い＝採用しない」ではない**ことが分かった事例である。
レオパレス（→ §16.19 前後・詳細設計書 §11.8）を落としたのは MUST 通過が
**構造的に0件**だったからで、ユニーク率の低さが理由ではない。
**順位に載るかどうかで判断する。**

